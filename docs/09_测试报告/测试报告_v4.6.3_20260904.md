# v4.6.3 测试环境部署验证报告（2026-09-04）

> 版本：v4.6.3（审批流体系补齐 SUBMITTER 节点 + 8 类业务审批挂载 + 系统开关管理员页 + 首页/合同工作台 UI 紧凑化 + Q1–Q4 首页/驾驶舱）
> Flyway：V148（幂等播种 6 个一层审批模板，无表结构变更）
> 环境：测试环境 dms-dev.mysolmed.com（43.128.145.141）；正式环境未推送（待指令）
> 来源：外部 changeset 包 `DMS-changeset-20260904/` 合回本地代码库（合并策略见 layer4 D39）

## 一、合并与构建

- 合并方式：逐文件 MD5+diff 比对——8 文件与本地 v4.6.2 相同（跳过）、6 新增直接拷、23 差异文件确认为干净增量后覆盖。
- 手工补 3 处包漏发（清单声明但未打包）：
  1. `backend/.../masterdata/service/SupplierService.java`：补 `SUPPLIER_CREATE` 创建审批挂载（@Lazy ApprovalService + afterCommit 发起 + 失败回退 active + 种子导入旁路）。
  2. `frontend-vue/src/views/authorization/api.js`：删除指向已移除端点 `/api/authorizations/order-enforce` 的死函数。
  3. `backend/src/test/.../AuthorizationServiceIntegrationTest.java:268`：被删方法 `authorizationService.isOrderAuthzEnforced()` 改读 `systemSettingService.isOrderAuthzEnforced()`，否则 test-compile 失败。
- 构建：后端 `mvn -o package -DskipTests` 通过（jar 117,197,681 字节）；`mvn -o test-compile` 通过；前端 `VITE_BASE=/dms/ npm run build` 通过（入口 index-B_cfV-yL.js，含 SystemSwitches chunk）。

## 二、部署

- 定向部署脚本 `scripts/deploy_v463_targeted.py`：只传后端 jar + 业务前端 dist（保留 admin 子目录），只 `force-recreate backend-test`，**未动 nginx/admin-vue/brochure**（铁律10）。
- 备份：`/opt/dms/backups/app-20260904-201607.jar`、`dms-front-20260904-201607`。
- 踩坑修复：V148 测试库已于当日 14:45 由前次服务器侧构建应用（checksum=61545495），本地 jar 解析 checksum=-1845694479，Flyway validate 失败致后端 502。核验 V148 为无 DDL、6 处 `IF NOT EXISTS` 幂等 DO 块且库中模板齐全后，`UPDATE flyway_schema_history SET checksum=-1845694479 WHERE version='148'` 对齐，重启恢复（详见 layer3 L82）。
- 结果：后端容器 `Up (healthy)`，`/actuator/health` 返回 `{"status":"UP"}`，Started DmsApplication 33.7s。

## 三、铁律9 GATE（真实浏览器，8 入口）

| 入口 | 结果 | title / 备注 |
|------|------|------|
| `/` | 302→/dms/login | 裸域名直达系统 |
| `/dms/` | 200 | MySolMed DMS 经销商管理系统 |
| `/dms/admin/` | 200 | 平台后台 |
| `/dms/mobile/login` | 200 | 移动 H5 登录 |
| `/dms/mobile/register` | 200 | 经销商准入注册 |
| `/brochure/` | 200 | 宣传 PC |
| `/brochure/mobile.html` | 200 | body 59013 字符，与 PC(133228) 不同，**非静默回退** |
| `/brochure/print.html` | 200 | body 1105，产品介绍页，非回退 |
| `/actuator/health` | 200 | `{"status":"UP"}` |

Console 0 红色 error；Network 0 个 5xx。

## 四、功能验证（登录后真实页面 + API + DB 回读）

- 登录：sys_admin / Dms@123456 / 租户 default，token 获取成功（表单字段顺序：租户代码/账号/密码）。
- 页面渲染（DOM 完整、无 404）：`/dms/home`（572 节点）、`/dms/dashboard`（1009）、`/dms/system-switches`（540，含「授权与下单挂钩」+ 定时邮件区）、`/dms/approval/templates`（1503，20 行模板）、`/dms/authorizations`（1135，含「终止」「续约」按钮）、`/dms/contracts`（1247）。
- API 回读：
  - `GET /api/system-switches` 200，返回 4 开关（order.authz.enforce=false、mail.schedule.enabled 等）。
  - `GET /api/tenant/features` 200，`inventoryEnabled:false`（Q2 库存隐藏开关生效）。
  - `GET /api/dashboard/top-dealers` 200，返回真实数据：苏州康宁医疗科技有限公司 16000、默认厂家经销商 15000（Q3 红冲过滤修复生效）。
- DB 回读（审批模板）：
  - ID28–33 六个模板（CONTRACT_TERMINATE/AUTHORIZATION_TERMINATE/AUTHORIZATION_RENEW/PRODUCT_CREATE/DEALER_CREATE/SUPPLIER_CREATE）全部 ENABLED。
  - 每个模板 1 个节点、2 个审批人，assignee 类型均为 `ROLE,SUBMITTER`（或签 ANY：管理员 ∪ 提交人本人）。
  - `approval_node_assignees` 中 SUBMITTER 共 14 条，非空（满足部署后审批人非空校验）。
  - ENABLED 模板总数 14。

## 五、遗留与后续

- 正式环境未推送（v4.6.3 + V148 仅测试环境）；待用户明确「推送正式环境」指令。
- 生产首次执行 V148 不受本次 checksum 对齐影响（新环境首次应用）。
- 浏览器如见旧界面，Ctrl+Shift+R 清缓存。
- 自动化脚本与截图：`automation_test/v463-results-20260904/`（gate_check.js、functional2.js、api_readback.js、截图 01–13）。
