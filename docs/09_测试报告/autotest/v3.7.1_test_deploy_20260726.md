# DMS v3.7.1 测试环境 · Fast 部署 + 自动化验证报告

**执行时间**：2026-07-26 13:14–13:17
**环境**：测试（8.133.193.238:8082/8083 · dms_test）
**版本**：v3.7.1（B01–B07 Bug 修复合入）
**执行者**：主 Agent（Dispatcher Guard）

---

## 一、部署耗时对比

| 部件 | v1 旧版 | Fast 首次 | Fast 缓存命中 | 提升 |
|---|---|---|---|---|
| Backend | 300–360s | 138s | ~30s | **~10×** |
| Frontend | 180–300s | 120s | 20s | **~10×** |
| **TOTAL** | **8–11 分钟** | **~260s** | **~50s** | 🎯 达到 90s 目标（缓存命中场景） |

日志：
- Backend cold: `健康探测 READY attempt=36 (~72s)`，构建 66s，同步 <5s
- Frontend cache-hit: `docker build Using cache × 11 steps`，构建 12s，容器起动 3s，proxy 首访 200

## 二、自动化验证

### API 冒烟（48 项 v3.7 全量）

| 类别 | 数量 | 明细 |
|---|---|---|
| ✅ OK 200 | **34** | v3.6.2 基线功能全部通过 |
| ❌ 500 | **0** | 无残留严重 Bug |
| ⚠️ 404（未实现） | **14** | v3.7 P0/P1/P2 需求缺口 |
| 错误 | 0 | — |

### UI 冒烟（Playwright）

| 端 | PC | 移动 |
|---|---|---|
| OK 页面数 | **35** | **7** |
| 5xx 页面 | 0 | 0 |
| 4xx | 1（"物料"，v3.7 未实现路由，预期） | 0 |

报告落地：`tools/browser-use/reports/api_smoke_20260726_131512.json`、`tools/browser-use/reports/ui_smoke_20260726_131535.json`

## 三、v3.7 需求缺口（14 项 404）

| 优先级 | 数量 | 代表条目 |
|---|---|---|
| P0 | 3 | US-B01 价格引擎（`/api/special-prices`）、US-D01 发票（`/api/invoices`）、US-D02 集成监控（`/api/integration/monitor`） |
| P1 | 8 | US-B02 返利池、US-B03 特殊价、US-B04 组套、US-B05 维修、US-C02 质量投诉、US-C03 CAPA、US-C04 追溯 |
| P2 | 3 | US-A02 医院-产品线、US-A03 快照、US-B06 常购模板、US-D03 预警 |

详见：`docs/09_测试报告/autotest/v3.6.2_prod_20260726.md`

## 四、部署 v2 关键改进

1. **服务器端构建**：Windows 本地无 Maven，`docker run maven:3.9` + 挂载 `/opt/dms/maven-repo` (176M) 全命中依赖缓存
2. **Docker layer cache 保留**：仅 jar 层重建，其余 10 层 `Using cache`
3. **增量同步**：tar 排除 target/node_modules，压缩后 <300KB
4. **服务器脚本增量清理**：`sync-extract.sh` 保留 `target/` 和 `node_modules/` 支持增量重编
5. **Health poll 兼容冷启动 72s**：上限 240s，10s/次分级输出
6. **SSH keepalive**：远端脚本 15–20s heartbeat 防 idle 断连
7. **修复 sh 脚本 BOM**：全部改用 .NET UTF8Encoding($false) 写入
8. **修复旧版 Docker 无 BuildKit**：移除 `--progress=plain`

## 五、修复的部署侧 Bug

- L20：`V31__inventory_unified_partial_cancel.sql` 引用不存在的 `code_prefixes` 表 → `DO $$ IF EXISTS ... $$` 包裹
- L21：SSH 长时间静默断连 → heartbeat
- L22：Docker 旧版无 BuildKit → 去掉 `--progress=plain`

## 六、下一步

1. ⏸️ **推送 v3.7.1 到生产**（需用户"推送正式环境"指令）
2. 📋 排期实现 14 项需求缺口（先攻 P0 3 项）
3. 🔧 服务器 Docker 升级到 20.10+（启用 BuildKit）
4. 🧪 引入 Full 档（`-FullBuild`），pom / package-lock 变更时使用

## 附录：本次修改的文件

**部署脚本**（新/改）：
- `.trae/skills/dms-deploy/deploy-fast.ps1`
- `.trae/skills/dms-deploy/remote/backend-fast.sh`
- `.trae/skills/dms-deploy/remote/frontend-fast.sh`
- `.trae/skills/dms-deploy/remote/sync-extract.sh`
- `.trae/skills/dms-deploy/SKILL.md`（重写为 v2）

**记忆库**（追加）：
- `.memory/layers/layer3-lessons.md` L20–L22
- `.memory/layers/layer4-decisions.md` D12

**清理的冗余文件**（根目录）：
- `backend-src.zip`, `frontend-src.zip`
- `create-backend-zip.py`, `create-frontend-zip.py`
- `deploy-dist-only.py`, `deploy-final.py`, `deploy-frontend-quick.py`
- `deploy-with-paramiko.py`, `deploy-frontend.ps1`

**服务器端修复**：
- `/opt/dms/dms-test/backend-src/src/main/resources/db/migration/V31__inventory_unified_partial_cancel.sql`