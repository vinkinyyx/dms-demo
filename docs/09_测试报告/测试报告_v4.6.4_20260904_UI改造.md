# v4.6.4 测试环境 UI 改造验证报告（2026-09-04）

> 版本：v4.6.4 浅色侧边栏菜单风格 + 库存模块关闭彻底清除库存痕迹 + 驾驶舱 TOP 经销商名称修复
> 类型：纯前端 PATCH，**无后端改动、无 Flyway 迁移、无 nginx 变更**
> 环境：测试环境 dms-dev.mysolmed.com；正式环境未推送

## 一、需求与实现
- R1 菜单浅色风格：`layout/index.vue` 侧边栏由深色渐变改为 `--dms-sider-*` token 驱动浅色扁平（白底、圆角 8px、选中浅蓝底+主色字）；`semantic.scss` 默认 sider token 改浅色；`theme-runtime.js` 明暗两套分支补齐 token，深色模式侧边栏不受影响。
- R2 库存痕迹清除（厂家用户 inventoryEnabled=false）：新增 `composables/useTenantFeatures.js`（单例缓存/去重/fail-open）；报表中心库存类分组、经销商画像库存明细 tab+2 个库存 KPI、驾驶舱库存三态区块/库存 KPI/效期横幅/活动图入库序列、命令面板库存项、报表订阅库存周转、漏标的寄售库存菜单，全部按开关隐藏。
- R3 TOP5 名称修复：echarts grid `containLabel` + y 轴类目名 width/ellipsis + 柱条金额标签 + tooltip 全名；TOP 医院图同法；KPI span 自适应（库存关时 8→5 张不留白）。

## 二、部署
- 前端 `VITE_BASE=/dms/ npm run build` 通过；`scripts/deploy_front_only.py` 只传 dist、仅覆盖 /opt/dms/test/frontend/dms（保留 admin），不重启后端、不动 nginx；备份 dms-front-20260904-212958。

## 三、铁律9 GATE（真实浏览器）
| 入口 | 结果 | title |
|------|------|-------|
| `/` | 200 | MySolMed DMS 经销商管理系统 |
| `/dms/` | 200 | 同上 |
| `/dms/admin/` | 200 | 平台后台 |
| `/dms/mobile/login` | 200 | 移动 H5 |
| `/dms/mobile/register` | 200 | 注册 |
| `/brochure/` `/mobile.html` `/print.html` | 200 | 宣传三页 title 正确 |

Console 0 红错、Network 0 个 5xx。

## 四、功能实测（sys_admin / default）
- R1：侧边栏背景 `rgb(255,255,255)`，菜单项文字深灰、选中项浅蓝圆角底 + 主色字；分组折叠正常。
- R3：驾驶舱「销售 TOP 经销商」5 家全名完整显示（合肥德医医疗器械有限公司 ¥440,962、天津金邦 ¥329,800、青岛海诺 ¥320,000、成都川医 ¥286,400、苏州康宁 ¥282,314），条形右侧带金额标签；KPI 5 张自适应排满。
- R2：驾驶舱图表区块仅余 销售趋势/销售TOP经销商/订单漏斗/医院手术TOP/近7日活跃（无库存三态、无合格库存 KPI、无效期横幅）；报表中心无「库存类」分组/库存周转/呆滞，hero 文案为「销售 · 订单 · 应收 · 报台 · 经销商画像」；经销商画像（DLR-011 天津金邦）tabs 仅 KPI概览/月度达成/返利明细/合同列表，无库存明细、无库存数量/SKU KPI，其他 KPI 正常；命令面板 Ctrl+K 搜「库存」无结果。
- 回归：登录、菜单展开、页面跳转、刷新均正常；无白屏/404。

## 五、遗留
- 生产未推送；本次纯前端，生产推送仅需部署前端 dist（后端/Flyway 不变）。
- 截图与脚本：`automation_test/v463-results-20260904/`（30-dashboard-final、31/33-dealer-profile、20-light-sidebar 等）。
