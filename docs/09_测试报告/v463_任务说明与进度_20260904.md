# 2026-09-04 全部任务说明与进度

> 版本：v4.6.3（需求 R1–R7）+ 当日追加首页/驾驶舱任务（Q1–Q4）
> 总体状态：**全部完成，测试环境（dms-dev.mysolmed.com）已部署并验证通过；正式环境未推送（待指令）**

---

## 一、v4.6.3 需求任务（R1–R7）

| 编号 | 任务 | 需求说明 | 状态 | 验证证据 |
|------|------|---------|------|---------|
| R1 | 首页快捷按钮紧凑重设计 | 首页快捷入口由大卡片改为紧凑图标网格 | ✅ 完成 | 前端构建产物 `Home-*.js` 含 `.ht-shortcuts` 紧凑网格；浏览器首页正常 |
| R2 | 合同工作台筛选风格统一 | 合同工作台筛选栏改为单行 inline、size=small，与其他菜单风格一致 | ✅ 完成 | 真实浏览器验证 `/dms/contracts`（35 条）单行紧凑筛选栏 |
| R3 | 合同终止流程 | 已生效合同可发起终止（终止原因必填），走审批；通过置 terminated，驳回/撤回恢复 effective | ✅ 完成 | 已生效行可见「终止合同」danger 按钮；ContractService + ContractApprovalCallback 落库；详情页同步入口 |
| R4 | 授权终止 + 授权续约 | 授权可终止、可续约（续约复制经销商+产品线+终端+新有效期），仅 active/not_started 状态行显示操作按钮 | ✅ 完成 | `/dms/authorizations`（38 条）：已到期/审批中行仅「查看」，生效中行有「查看/终止/续约」；独立业务类型 AUTHORIZATION_RENEW |
| R5 | 「授权与下单挂钩」开关收口 | 开关从授权管理页摘除，迁至「用户与权限」下管理员专属「系统开关」页；仅租户管理员可访问 | ✅ 完成 | `/dms/system-switches` 面包屑「首页/用户与权限/系统开关」；挂钩开关=关闭（解耦默认）；`/api/system-switches` 类级 `@perm.isTenantAdmin()`；order-enforce 旧端点已移除 |
| R6 | 定时系统邮件开关收口 | 定时邮件 3 个开关（mail.schedule.enabled/report/approval）同样收口到管理员系统开关页 | ✅ 完成 | 系统开关页定时邮件 3 开关=开启；SystemSwitchController GET 聚合 4 开关、POST 白名单分流、非法 key 返回 40001 |
| R7 | 8 类业务审批流挂载 | 合同创建/合同终止/授权创建/授权终止/授权续约/销售订单条件审批/销退订单/物料·经销商·供应商创建，共 8 类挂载审批引擎；测试环境一层或签、审批人=提交人本人 ∪ 系统管理员 | ✅ 完成 | V148 播种模板 ID28–33 全部已启用（共 33 条模板）；审批引擎新增 SUBMITTER 节点类型，三分支（USER/SUBMITTER/ROLE）闭环且安全核查通过；销售订单仅产品全局+经销商全局折扣免审，其余成交价差异一律送审；主数据待审期间不可选用 |

**澄清口径（已与用户确认）**：①促销命中也要审批（成交价与基础价任何差异都审，仅产品全局折扣+经销商全局折扣自动免审）；②主数据待审期间不可选用、审批通过后生效；③引擎新增 SUBMITTER 节点类型 + 授权续约独立业务类型 AUTHORIZATION_RENEW；④Flyway 播种一层模板 +「用户与权限」下新建管理员专属「系统开关」页。

## 二、当日追加任务（Q1–Q4，首页 / 数据驾驶舱）

| 编号 | 任务 | 需求说明 | 状态 | 验证证据 |
|------|------|---------|------|---------|
| Q1 | 首页快捷入口上移 | 将快捷入口从首页底部移至页面顶部 | ✅ 完成 | Home.vue 快捷入口位于顶部 topbar `.ht-shortcuts`；部署产物 grep 命中 ht-shortcuts/visibleShortcuts |
| Q2 | 库存模块关闭时隐藏库存相关内容 | 库存模块未启用时，首页与驾驶舱隐藏所有库存相关报表/数据（KPI 卡片、图表、快捷统计、快捷入口、效期预警横幅） | ✅ 完成 | 前端 onMounted 拉 `/api/tenant/features`，测试环境返回 `inventoryEnabled:false`；Home.vue visibleShortcuts/visibleQuickStats、Dashboard.vue VISIBLE_BLOCKS 按 inventoryOnly 过滤；饼图按开关条件请求；部署产物 chunk 含 inventoryEnabled 标记 |
| Q3 | 修复 TOP5 经销商图表无数据 | 「TOP5 经销商」图表不显示数据 | ✅ 完成 | 根因：topDealers SQL 缺少红冲单过滤（KPI totalSales 有 `is_red` 过滤而 TOP5 没有），红冲单干扰聚合。已在 DashboardController.topDealers 补 `(o.is_red IS NULL OR o.is_red = false)` + 时间筛选；接口实测返回真实数据（苏州康宁 16000、默认厂家 15000）；新 jar 内含 2 处 is_red 过滤 |
| Q4 | 数据驾驶舱布局紧凑化 | 驾驶舱（Dashboard）布局紧凑化，风格向首页看齐，**保留拖拽排序功能** | ✅ 完成 | Dashboard.vue 紧凑样式（区块间距 8px、KPI 字号 20px、图表高度 220px）；vuedraggable 拖拽、布局保存/重置逻辑保留（orderedBlocks/loadLayout/resetLayout 改用 VISIBLE_BLOCKS） |

## 三、部署与验证记录（测试环境，2026-09-04）

- **构建方式**：本机终端桥故障（公司电脑 PATH 永久损坏，见 project_rules.md §14），全程走服务器侧 Maven/Node Docker 构建 + SSH MCP（ssh2 密码认证）运维。
- **后端**：jar 117,197,075 字节；本会话替换 `/opt/dms/test/backend/app.jar`（旧版备份 `app.jar.bak-20260904-172803`），容器重启后健康检查 UP；新 jar 校验含 2 处 is_red 过滤。
- **前端**：`VITE_BASE=/dms/` 构建（198 assets，含 SystemSwitches chunk；本会话产物入口 `index-DPL3-cEd.js`，chunks `Dashboard-CBnYjSbf.js`、`Home-Wmsglu2T.js`）；部署至 `/opt/dms/test/frontend/dms/`（admin/ 子目录保留不动）；dist 内 grep 确认新代码标记。
- **数据库**：V147 checksum 对齐修复（库 1561521515 → 本地 906988248，先核验 V147 操作幂等已生效再 UPDATE flyway_schema_history）；**V148 迁移成功，Current=148**。
- **铁律9 真实浏览器 GATE**：`/`、`/dms/`、`/dms/admin/`、`/dms/mobile/login`、系统开关页、审批模板页、授权管理页、合同工作台等入口逐项 200；Console 无红错（仅 1 条 Element Plus el-radio label 弃用 warn，历史既有）；Network 无 5xx。
- **端到端 API（经前端端口）**：`POST /api/auth/login` 200 取 token → 产品列表 200 → top-dealers 返回真实数据 → `/api/tenant/features` 返回 inventoryEnabled:false。
- **Nginx 代理**：测试环境前端 proxy_pass 指向 `backend-test:8080`（容器网络别名），配置核验无误。
- **清理**：服务器临时构建脚本、未使用的 dms-frontend-test 镜像、Docker 构建缓存已清理；本地仓库根目录一次性脚本（deploy-all-test.ps1、fix.sh）已删除。

## 四、未完成 / 待指令事项

| 事项 | 状态 | 说明 |
|------|------|------|
| 推送正式环境 | ⏸ 待指令 | v4.6.3 + Q1–Q4 均仅在测试环境验证；按双环境规则，需用户明确「推送正式环境」后执行 |
| 正式环境 Flyway V148 | ⏸ 待推送 | 正式库当前版本随正式推送时迁移 |
| 浏览器强制刷新提醒 | ℹ️ | 测试环境验证后如见旧版界面，Ctrl+Shift+R 清浏览器缓存 |

## 五、关键假设

1. 测试环境 nginx 以 bind-mount 方式提供前端静态文件与后端 jar，无前端容器；归档与部署均以此拓扑为准。
2. 今日「删除文件」仅涉及本地一次性脚本；源码层面无删除（AuthorizationController 为方法级移除，文件保留）。
3. 变更清单旧稿中 PermissionChecker 路径 `system/service/` 为笔误，实际路径 `security/PermissionChecker.java`，本包按实际路径归档。
