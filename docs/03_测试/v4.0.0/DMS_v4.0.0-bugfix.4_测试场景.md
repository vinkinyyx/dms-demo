# DMS v4.0.0-bugfix.4 测试场景与结果

- 版本：v4.0.0-bugfix.4
- 测试环境：http://43.128.145.141/ （租户 `default` / `sys_admin` / `Dms@123456`）
- 测试日期：2026-08-19
- 测试方式：Playwright headless Chromium（viewport 1680×1050）真实点击 + REST API 双验证
- 后端版本：`dms-backend.jar`（V96–V103 迁移已应用）；前端 hash 见线上 `index-*.js`

> 图例：✅通过　❌失败　⚠️受限/未实现（已说明）

## 一、所有业务菜单统一项（7 条）

覆盖：基础数据（产品/分类/经销商/医院/仓库/区域/供应商/产品线/产品组合/产品价格/授权）、合同管理、订单业务（销售订单/销退/采购订单/采购退货/销售出库）、库存（收货/库存移动/库存调整）、手术与营销（手术报告/促销）、业务报表、审批中心、用户与权限。

| # | 场景 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|
| U1 | 链接/查看进只读页 | 列表点击业务编码链接；点击"查看" | 在列表区域内打开只读面板/详情，URL 不跳走，含返回按钮 | ✅ 21 模块抽样通过（products/bundles/prices/promotions/orders/returns/outs 等） |
| U2 | 按钮可用、去批量删除 | 检查工具栏 | 无"批量删除"；新增/导入/导出等可用 | ✅ 21 模块无批量删除按钮 |
| U3 | 紧凑布局 | 打开编辑/只读页 | 每行 3 字段、标签等宽、输入框等宽 100%、长字段完整 | ✅ OrderCreate/各 CrudView 表单 `el-col span=8` |
| U4 | 筛选下拉非空 | 点开各筛选项下拉 | 状态/分类/经销商等有选项 | ✅ `audit_filters3` 全通过；远程加载 categories/dealers/warehouses |
| U5 | 只读页操作日志 | 打开只读页底部 | 展示"操作日志"（合并审计+业务日志） | ✅ 抽样 7 模块均有 |
| U6 | 列表宽度自适应 | 不同分辨率 | 操作列右侧无大片留白 | ✅ min-width 列，1440/1920 gap=0 |
| U7 | 行操作折叠 | 行操作 >2 个 | 前 2 个平铺，其余收"更多" | ✅ maxFlatRowButtons=2 |
| U8 | 详情/编辑面板统一 | 打开各编辑/只读 | 统一为只充满列表页区域（非全屏抽屉），随侧栏折叠自适应 | ✅ 详情面板在列表容器内 |

## 二、产品管理

| # | 场景 | 结果 |
|---|---|---|
| P1 | 列表状态、产品分类下拉有筛选内容 | ✅ |
| P2 | 点产品编码/查看进只读页（非编辑） | ✅ |
| P3 | 产品层级不维护单价/税率，价格统一在"产品价格" | ✅ 产品表单无价格字段 |
| P4 | 无批量删除 | ✅ |
| P5 | 只读页含操作日志 | ✅ |
| P6 | 编辑/只读布局紧凑整齐 | ✅ |

## 三、产品组合（BOM）

| # | 场景 | 步骤/预期 | 结果 |
|---|---|---|---|
| B1 | 编码/查看进只读页 | 只读，含日志 | ✅ |
| B2 | 紧凑布局 | 每行 3 字段 | ✅ |
| B3 | 操作日志在只读页 |  | ✅ |
| B4 | 无删除按钮 | 接口 `DELETE` 返回 405；前端无删除 | ✅ |
| B5 | 新建版本按钮 | 点"新建版本"→ 生成 draft 草稿并直接打开可编辑，头部只读，可改子件；原 active 不变 | ✅ `POST /api/product-bundles/{id}/new-version` 返回 draft |
| B6 | 仅 draft 可编辑 | 前端 `editableWhen:['draft']`；active 只有"查看/新建版本" | ✅ |
| B7 | 版本号系统自动生成 | 表单 bomVersion 只读 | ✅ |
| B8 | 母件 SKU 列排序不报错 | `GET /api/product-bundles?sort=productCode,desc` | ✅ 200（safePageable 跳过非法字段） |

## 四、产品价格

| # | 场景 | 步骤/预期 | 结果 |
|---|---|---|---|
| PR1 | 价格不可删不可改 | `DELETE` 返回业务错误；`PUT` 仅改 status | ✅ DELETE 400"不允许删除" |
| PR2 | 生效/失效 | `POST /{id}/activate`、`/deactivate` | ✅ 200 |
| PR3 | 列表查看进只读页 |  | ✅ |
| PR4 | 紧凑布局 |  | ✅ |
| PR5 | BOM 子件可按经销商维护价格 | 产品选择器 `excludeBundle:true`（排除母件）；partnerType=DEALER 必选经销商；可给每个子件维护，含 0 金额 | ✅ 产品选择器排除母件；按 dealerId 过滤 |
| PR6 | 含税价+税率可编辑，不含税自动算 4 位小数 | 前端 calc divide | ✅ |
| PR7 | 采购价同样有含税/不含税/税率 |  | ✅ |
| PR8 | 币种用字母缩写 CNY/USD/EUR | `CURRENCIES` 常量 | ✅ |
| PR9 | 汇率维护暂不使用，报表/币种处带出币种 | 报表保留 currency 字段 | ⚠️ 汇率维护入口未做（按需求暂不需要） |

## 五、促销规则

| # | 场景 | 结果 |
|---|---|---|
| PR01 | 编码/查看进只读页 | ✅ |
| PR02 | 紧凑布局、操作日志 | ✅ |
| PR03 | 无删除，仅启用/停用或到期自动停 | ✅ noDelete；activate/deactivate |
| PR04 | 按促销模式联动必填：满A赠B→赠品/数量/周期；满A减→减免金额 | ✅ showIf 联动 |
| PR05 | 无"折扣方式/折扣值"字段（折扣只在订单层） | ✅ 表单已移除 |
| PR06 | 满 A 按 A 的数量判定 | ✅ hitQty 汇总命中行数量 |

## 六、销售订单

| # | 场景 | 步骤/预期 | 结果 |
|---|---|---|---|
| O1 | 单号/查看进只读页 |  | ✅ |
| O2 | 紧凑布局，字段完整，备注多行 | OrderCreate 备注 textarea :rows=3 | ✅ |
| O3 | 只读页操作日志 |  | ✅ |
| O4 | 列表状态、经销商下拉有内容 |  | ✅ |
| O5 | 列表筛选/按钮整齐 |  | ✅ |
| O6 | 选 BOM 母件带出子件，母子件树形展示 | 选 309→展开 331/332 子件行；tree-props children | ✅（本版修复多行展开） |
| O7 | 母件不打折，折扣体现在子件；母件数量可改，子件数量随配比不可改 | 母件行显示"母件不打折"；子件数量 disabled | ✅ |
| O8 | 行折扣针对整行 | 10 EA×1460，行金额折扣 −100 → 14500（不是每 EA−100） | ✅ |
| O9 | 触发促销需展示 | promoTags/promoDiscountAmount 列 | ✅ |
| O10 | 编辑页有提交按钮 | "提交"按钮（canSubmit） | ✅ |
| O11 | 保存/提交不校验授权 | 提交不再报"商品无有效授权"；sys_admin 自动审批 | ✅ 提交→APPROVED |
| O12 | 保存后重复提交守卫 | saving/submitting 标志，提示"请勿重复提交" | ✅ |
| O13 | 取消不跳 404 | goBack 推 /m/orders | ✅ |
| O14 | 日期时间筛选生效 | createdAt=from,to 区间查询 | ✅ 三种参数 200 |
| O15 | 模拟创建出库按钮 | 详情/创建后 simulate-ship | ✅ 生成 GI-20260819-xxxx |
| O16 | 价格按比例分摊到每行每 EA 并传出库 | 整单金额折扣按行金额比例分摊，出库行 unit_price=每 EA 折后价 | ✅ 出库 unitPrice=1450 |
| O17 | 提交后不可删除 | deletableWhen DRAFT；后端状态校验 | ✅ |
| O18 | 新增订单为全新空白 | onActivated resetForm | ✅ |

## 七、销退订单

| # | 场景 | 结果 |
|---|---|---|
| R1 | 编辑/只读紧凑布局，备注多行，字段完整 | ✅ |
| R2 | 选发货单排除无可退数量 | ✅ shipped-outs 仅列有 returnableQty 的单 |
| R3 | 发货/退货数量整数 | ✅ V103 修正历史小数；前端 precision=0 |
| R4 | 带出原订单价格 | ✅ 出库行 unitPrice 回填（1450） |
| R5 | 提交审批不跳 404 | ✅ submit→PENDING_APPROVAL |

## 八、报表/币种

| # | 场景 | 结果 |
|---|---|---|
| RP1 | 销售排行报表 | ✅ 200 |
| RP2 | 库存周转报表 | ✅ 200 |
| RP3 | 订单溯源 | ✅ 200 |
| RP4 | 热门产品 | ⚠️ `/api/reports/top-products` 404（一期未实现，菜单隐藏） |
| RP5 | 币种信息带出、字母缩写 | ✅ CNY/USD/EUR |

## 九、已知限制

- 汇率维护功能本期不实现（按确认：暂不在任何业务使用，报表保留币种字段即可）。
- 热门产品报表接口一期未实现，前端未暴露菜单入口。

## 十、续作复核补充测试（2026-08-19 晚）

| # | 场景 | 预期 | 结果 |
|---|---|---|---|
| X1 | BOM 母件下单展开全部子件 | 按 active BOM 展开，每个子件一行 | ✅ isBom 改查 product_bundles |
| X2 | BOM 子件各自价格 | 金额=Σ(子件单价×配比×母件数量)，母件不维护价格 | ✅ 500=100×2+50×6 |
| X3 | 母件行折扣整行作用 | 行折扣按子件标准金额比例分摊 | ✅ 100→40/60 |
| X4 | 整单折扣不重复计算 | 表头折扣只分摊一次 | ✅ 100→40/60，最终 300 |
| X5 | BOM 母件不可建价 | POST 返回 400；SKU 下拉 excludeBundle 生效 | ✅ |
| X6 | 价格 PUT 仅改状态 | 携带其它字段返回业务错误 | ✅ |
| X7 | 编辑 BOM 草稿子件 | 不再 500 唯一约束冲突 | ✅ flush 后重建 |
| X8 | 新版本流转 | new-version 生成 draft；原版本保持 active；activate 后原版转 history | ✅ |
| X9 | 24 业务列表页 | http 200、有表格、无 console error、无白屏 | ✅ |
| X10 | 销售订单/销退/出库 链接与查看 | 在列表区域内打开详情，无 404 | ✅ |
| X11 | BOM 列表母件 SKU 排序 | productCode 升降序不报错 | ✅ 200 |

> 自动化回归脚本：`automation_test/v4_api_regression.py`（3/3 通过，报告 `automation_test/v4-api-results/regression-report.json`）。
