# DMS v3.12.4 全量回归 + 三端深度E2E 测试报告

> 测试日期：2026-08-15
> 测试环境：http://43.128.145.141/ （健康检查 `{"status":"UP"}`）
> 版本基线：v3.12.3 P2 基础上修复采购提交性能与邮件日志编译问题，部署为 v3.12.4
> 测试方式：pytest 自动化全量回归 + Playwright 浏览器深度遍历三端
> 部署人：Codex（通过 scripts/deploy_test.py，备份戳 20260815-012142）

---

## 1. 本轮修复内容

### 1.1 真实 Bug（后端）
| Bug | 现象 | 根因 | 修复 | 文件 |
|---|---|---|---|---|
| 采购订单提交耗时 51.3s | `POST /api/purchase-orders/{id}/submit` 阻塞约 51 秒才返回 | 审批通知邮件 `ApprovalMailNotifier` 同步 `mailSender.send()`，SMTP 连接慢拖垮业务请求 | 4 个发送方法加 `@Async`；新增默认异步线程池 `taskExecutor`；SMTP 加 connection/write/read 5s 超时 | `notification/mail/ApprovalMailNotifier.java`、`asynctask/config/AsyncTaskExecutorConfig.java`、`application.yml` |
| 后端无法编译 | `mvn compile` 报 `getRetries()/setRetries()/setDurationMs()/getById()` 找不到 | V95 邮件日志迁移给 `email_logs` 加了 `retries/duration_ms/template_code` 列，但实体与服务未同步 | `EmailLog` 补 `retries/durationMs` 字段；`EmailLogService` 补 `getById` | `notification/entity/EmailLog.java`、`notification/service/EmailLogService.java` |

修复后采购订单提交耗时：**51.3s → 0.67s**。

### 1.2 自动化脚本修复（测试端，非系统缺陷）
- `tests/test_p2_features.py`：盘点详情返回结构为 `{stocktake, lines}`，断言改为兼容包裹/扁平两种结构。
- `utils/api_client.py`：登录/验证类请求在遇到 429 时自动退避重试（DMS_AUTORETRY_429，默认开启），消除全量跑的登录限流级联。
- `tests/test_p2_features.py`：登录爆破限流测试改用独立 `X-Forwarded-For`，不再污染共享 IP 的登录配额。
- 新增三端深度 E2E 脚本 `tools/p2-e2e/deep-e2e.cjs`、`deep-mobile-admin.cjs`（PC 34 菜单 + 平台 9 页面 + 移动 7 Tab，含登录、404/500、控制台错误检测）。

---

## 2. 构建与部署

| 项 | 命令 | 结果 |
|---|---|---|
| 后端 | `mvn -DskipTests package` | 通过，产出 `backend/target/dms-backend.jar`（116 MB） |
| 业务前台 | `npm --prefix frontend-vue run build` | 通过 |
| 平台后台 | `npm --prefix admin-vue run build` | 通过 |
| 部署 | `python scripts/deploy_test.py` | 通过，backend/nginx 容器 force-recreate，健康检查第 7 次探测 200 UP |

部署后验证：`POST /api/purchase-orders/{id}/submit` 返回 200，新状态 PENDING_APPROVAL。

---

## 3. 自动化全量回归（pytest）

- 命令：`pytest tests/ -q -p no:xdist`（DMS_ENV=test，DMS_AUTORETRY_429=1）
- 结果：**548 passed, 39 skipped, 23 xfailed, 22 xpassed，0 failed，0 errors**，耗时 71.38s
- 对比修复前基线：518 passed / 2 failed / 31 errors（登录限流级联 + 采购提交超时）→ 全部清零。

覆盖模块（26 个测试文件、632 用例）：登录、产品/分类/供应商/仓库/医院、合同、销售订单、采购订单、库存/出入库/调整、手术报台、促销、6 类报表、审批流/委托/监控、账号/角色/岗位、平台租户/字典/菜单/日志、移动端 H5、P0/P2 特性（MFA/限流/盘点/报表订阅/效期/追溯）、安全（SQL 注入/XSS/越权/Token 隔离/脱敏）、L1-L5 五层链路。

---

## 4. 浏览器深度 E2E（Playwright）

### 4.1 P2 专项（p2-e2e.cjs）
16 项检查全部通过，0 控制台错误：登录、报表订阅 CRUD/启停/删除、库存盘点页与上传弹窗、MFA 启用、效期预警、序列号追溯、移动扫码收货/库存扫码。

### 4.2 三端全菜单遍历
| 端 | 覆盖 | 结果 |
|---|---|---|
| PC 业务前台 | 34 个菜单/路由（工作台、主数据、订单、库存、盘点/效期/追溯、合同、报表、驾驶舱、产品对码、审批中心四项、用户权限、日志、导入导出任务） | 全部 200 渲染，无 404/500，无 JS 错误 |
| 平台后台 | 9 个页面（厂家/经销商租户、租户管理员、角色模板、平台菜单、页面配置、全局字典、接口日志、审计日志） | 全部 200 渲染，无 JS 错误 |
| 移动端 H5 | 7 个 Tab/页（首页、订单、报台、我的业绩、移动审批、消息中心、我的） | 全部 200 渲染，无 JS 错误 |

注：移动报台列表当前显示“暂无报台”（数据为空，非缺陷）；审批 Tab 已上线（之前记忆中“4 Tab 缺审批”的问题已不复现）。

---

## 5. 结论

v3.12.4 修复已构建、部署并通过自动化全量回归与三端浏览器深度 E2E：

- 自动化：548 passed / 0 failed / 0 errors。
- E2E：PC 34 + 平台 9 + 移动 7 全部通过，0 控制台错误；P2 专项 16/16 通过。
- 关键性能问题（采购提交 51s）已解决（0.67s）。
- 邮件日志模块编译阻断已解决。

已知非阻断项：
- SMTP 在测试环境偶发连接慢，已通过异步 + 5s 超时隔离，不影响业务提交；生产建议配置可用的 SMTP 或设置 `dms.mail.enabled=false`。
- 部分 P1/P2 规划功能（深度返利、数据大屏、电子签章、离线模式等）仍在 backlog，见 `docs/01_需求/DMS需求文档_汇总版.md` 附录 A。

---

## 6. 产物

- 自动化 HTML 报告：`automation_test/reports/report.html`
- E2E 结果：`tools/p2-e2e/results/deep-e2e-results.json`、`p2-e2e-results.json`
- 部署备份：服务器 `/opt/dms/backups/app-20260815-012142.jar`、`frontend-20260815-012142/`
