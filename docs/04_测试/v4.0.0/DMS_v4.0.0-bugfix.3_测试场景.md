# DMS v4.0.0-bugfix.3 测试场景（业务菜单统一调整）

本文档针对业务菜单统一 7 项规范，设计覆盖所有业务菜单的测试场景。

## 通用前置
- 登录：租户 `default` / 账号 `admin` / 密码 `Sh123456`，地址 http://43.128.145.141/
- 每个菜单进入列表页后逐项检查以下 7 条。

## 七项通用检查项

| 编号 | 检查点 | 期望结果 |
|---|---|---|
| C1 | 点击业务编码链接 | 进入只读页/只读抽屉，页面无"保存"按钮 |
| C2 | 点击行"查看"按钮 | 同上，进入只读 |
| C3 | 点击"编辑"按钮 | 才进入可编辑表单 |
| C4 | 工具栏"批量删除" | 所有菜单均不出现批量删除 |
| C5 | 工具栏其他按钮（查询/重置/新增/导入/导出等） | 可点击、有响应、无 JS 报错 |
| C6 | 编辑页/只读页布局 | 每行 3 字段，标签/输入框等宽，长字段完整显示不截断 |
| C7 | 筛选下拉 | 状态/分类/经销商/仓库等下拉均有选项，不为空 |
| C8 | 只读页操作日志 | 底部展示"操作日志"（无数据时显示"暂无操作日志"） |
| C9 | 列表宽度 | 表格随浏览器宽度自适应，操作列右侧无大片留白 |
| C10 | 行操作按钮 | 超过 2 个时，前 2 个平铺、其余收入"更多"下拉 |

## 分菜单场景

### 基础数据
- 产品管理 / 产品分类 / 产品线 / 产品组合(BOM) / 经销商 / 医院终端 / 仓库 / 区域 / 供应商 / 产品价格
- TC-MD-01 列表点产品编码→只读，无保存；C1~C10 全通过
- TC-MD-02 产品价格只读页无编辑/删除，行操作为"生效/失效"
- TC-MD-03 BOM 行操作为"查看/新建版本"（编辑在更多中），版本号系统自动生成
- TC-MD-04 状态、分类、仓库等筛选下拉均有选项

### 合同管理
- 合同工作台 / 合同模板 / 授权管理
- TC-CT-01 合同编号链接→合同只读详情，底部有"操作日志"
- TC-CT-02 合同行操作（查看/编辑/提交/删除/撤回）超过 2 个折叠到"更多"
- TC-CT-03 合同模板、审批流配置行操作超过 2 个折叠
- TC-CT-04 授权管理走通用列表，C1~C10 通过

### 订单业务
- 销售订单 / 销退订单 / 采购订单 / 采退订单
- TC-OD-01 单号链接/查看→只读，无保存；操作日志存在
- TC-OD-02 状态、经销商筛选下拉非空；日期筛选生效
- TC-OD-03 销售订单行操作（查看/取消…）超过 2 个折叠
- TC-OD-04 新建销售订单含保存+提交，无发货仓库字段；BOM 选择带"BOM组套"标签

### 库存业务
- 库存查询 / 销售出库 / 收货入库 / 库存移动 / 库存调整 / 效期预警 / 库存盘点 / 序列号追溯
- TC-IV-01 库存查询"仓库/库存状态"筛选下拉非空
- TC-IV-02 各列表查看进入只读，操作按钮（确认/打开等）≤2 平铺或折叠
- TC-IV-03 效期预警、盘点、追溯自定义页面无 JS 报错、布局正常

### 手术与营销
- 手术植入报台 / 促销规则
- TC-SP-01 促销规则只读，无删除，行操作为"查看/停用"（启用），编辑在更多中
- TC-SP-02 手术报台列表/只读布局正常、日志存在

### 业务报表 / 产品对码
- 报表中心、报表订阅、产品对码
- TC-RP-01 报表订阅行操作（立即发送/编辑/启用停用/删除）折叠
- TC-RP-02 产品对码、报表页面加载无报错

### 审批中心
- 我的审批 / 审批流配置 / 审批委托 / 审批监控
- TC-AP-01 审批流配置行操作（编辑/发布/停用/新版本）折叠
- TC-AP-02 各页面加载无 JS 错误

### 用户与权限
- 销售岗位 / 账号管理 / 角色权限 / 列表页配置 / 日志中心 / 导入导出任务
- TC-UC-01 账号/角色走通用列表，C1~C10 通过
- TC-UC-02 岗位、列表页配置、日志中心、异步任务页面加载无报错

## 自动化回归结果（2026-08-18 测试环境）

| 套件 | 结果 |
|---|---|
| audit_menus.py（25 模块通用检查） | 25/25 通过 |
| audit_filters3.py（24 模块下拉非空） | 全部通过 |
| smoke_all.py（19 页面无 JS 错误） | 0 pageerror |
| detail_logs.py / drawer_logs.py（只读+日志） | 通过 |
| width.py（1440/1920/2560 宽度自适应） | gap=0 |
| v4_api_regression.py | 3/3 通过 |
| browser_v4.py | 13/13 通过 |
