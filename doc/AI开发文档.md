# AI 开发文档（快照指针）

> 本文件为跨会话快速加载入口。**本项目文档主目录为 `docs/`**，完整 AI 上下文快照维护在：
>
> **👉 [../docs/AI开发文档.md](../docs/AI开发文档.md)**（技术栈、目录结构、编码规范、模块信息、第三方依赖、历次修改记录、踩坑点、本地启动配置）
>
> **维护约定**：完整内容在 `docs/AI开发文档.md` **单点维护**，本文件只保留指针 + 当前版本速览；修改文档时先改 `docs/` 主文档，再同步本文件的版本速览与变更要点，禁止在本文件内展开全文（防止两份副本内容漂移）。

## 当前版本速览（2026-09-04）

- 当前版本：**v4.6.3**（**MINOR，审批流体系补齐 + 系统开关管理员页 + UI 紧凑化，Flyway V148，已部署测试环境并通过铁律9 浏览器逐项验证**）。对应用户 7 项需求：
  - **审批引擎新增 SUBMITTER 节点类型（提交人本人）**：ApprovalAssigneeType = USER/ROLE/**SUBMITTER**；ApprovalService.resolveNodeUsers 三分支闭环（SUBMITTER 按 submitterId 取提交人本人，均 filter 同租户+active，LinkedHashMap 去重）；配合节点 ANY 或签，**审批人=提交人本人∪系统管理员，一层即可**。
  - **8 类业务审批挂载**：①合同创建 CONTRACT（既有）②**合同终止 CONTRACT_TERMINATE**（已生效合同、终止原因必填）③授权创建 AUTHORIZATION（既有）④**授权终止 AUTHORIZATION_TERMINATE** ⑤**授权续约 AUTHORIZATION_RENEW（独立业务类型，复制经销商+产品线+终端+新有效期）**；④⑤**状态门禁：仅 active(生效中)/not_started(未开始) 可终止/续约**，其余状态后端拒绝、前端按钮不渲染；⑥**销售订单条件审批**——成交价与基础价任何差异（促销/特价/整单折扣/手工改价）即审，**仅产品全局折扣+经销商全局折扣自动免审**；⑦**销退单 RMA 一律审批**；⑧**物料/经销商/供应商创建审批**——pending_approval 待审期间不可被业务单据选用，通过后生效。
  - **系统开关管理员页（需求⑤⑥）**：SystemSwitchController `/api/system-switches` 聚合 4 开关（租户级 order.authz.enforce 授权与下单挂钩 + 全局 mail.schedule.enabled/report/approval 3 个定时邮件开关），类级 `@perm.isTenantAdmin()` 仅租户管理员；授权挂钩开关从授权管理页摘除、迁至「用户与权限」下新增 SystemSwitches.vue；旧 `/api/authorizations/order-enforce` 端点已移除。
  - **Flyway V148**（checksum=61545495）：幂等播种 6 个一层模板（ID28 合同终止/29 授权终止/30 授权续约/31 物料创建/32 经销商创建/33 供应商创建，各 1 节点 ANY + SUBMITTER&ROLE 管理员），无表结构变更；测试环境 Current=148。
  - **前端 13 文件**：SystemSwitches.vue（新）、authorization/api.js+AuthorizationList.vue（切新端点/摘开关/终止续约门禁按钮）、approval dict.js+AssigneePicker+ApprovalFlowEditor+ApprovalTemplates（SUBMITTER 选择器）、contract ContractWorkspace+ContractDetail+api（终止合同按钮+原因必填）、ContractWorkspace **单行 inline size=small 紧凑筛选栏**（f2）、Home.vue **快捷按钮紧凑重设计**（f1）、menu.js+router（/system-switches）。
  - **UI 紧凑化**：首页快捷入口由大卡片改紧凑图标网格（需求①）；合同工作台筛选/操作风格对齐其他菜单（需求②）。
  - **部署验证（2026-09-04）**：服务器侧 Maven/Node 构建（本机终端桥故障 §14）；V147 checksum 对齐修复（1561521515→906988248，先核验幂等再对齐）、V148 迁移成功；铁律9 真实浏览器——系统开关页（挂钩=关闭解耦、邮件 3 开关=开启）、审批模板页（V148 六模板已启用、共 33 条）、授权页（终止/续约仅生效中行可见、无解耦开关）、合同工作台（紧凑筛选栏+终止合同按钮）、Console 无红错、H5 正常。
- 上一版本：v4.6.2（**授权产品线化重构**：授权台账/终端/产品线管理，引入 order.authz.enforce 租户开关——v4.6.3 已迁至 /api/system-switches 管理员页；Flyway **V147**）；v4.6.1（**PATCH，定时邮件发送运行时开关**：system_settings 动态开关——总开关 mail.schedule.enabled + 报表订阅/审批提醒两个子开关，DB 值优先、yml `dms.mail.enabled` 兜底、Redis 30 分钟缓存；平台后台「平台配置 → 通知设置」页可视化切换、即时生效无需重启；Flyway **V146** 种子 3 个开关；**仅控制定时自动邮件（每日 08:00 报表 CSV、09:00 审批超时提醒），业务即时邮件不受影响**。开发期曾标号 v4.5.7，因 v4.5.7 已分配给移动端 H5 重设计、v4.6.0 已分配给测试框架重构，正式定版 v4.6.1）；v4.6.0（**统一测试框架重构**：每层唯一工具——后端 JUnit5+Mockito+zonky 内嵌 PG14+Testcontainers 真实 Redis7（*IT）+JaCoCo，前端 Vitest+money.js，黑盒/三端 UI/部署 GATE 统一 Playwright（api/pc/admin/mobile/gate 5 project），k6 性能、OWASP ZAP 安全；纯测试基建/依赖/文档，不改业务代码，无 Flyway）；v4.5.7（**移动端 H5 简约重设计**：首页重构 + tabbar 6→5 + 全站蓝色单色视觉归一 + BarcodeScanner 扫码组件，纯前端无 Flyway，已前端-only 部署测试环境）
- 更早版本：v4.5.6（**PATCH 补丁**：① Flyway **V145** 经销商租户 button 权限资源补全——修复新开户经销商租户登录后业务菜单全不可见/业务路由 403 的平台级 RBAC 缺陷（TenantRoleProvisioner 开户只建 menu 不建 button，前端菜单显隐检查 button 码）；以模板租户 DEALER_A1 的 215 个 button 为基准幂等复制到所有缺 button 的 DEALER 租户并绑定 DEALER_ADMIN 策略 {view}；② 跨租户协同收货单号 RK→GR 修复（onSalesOutShipped 取号 next("GR")、红字 next("RGR")）；测试环境联调 2 经销商采购→厂家发货全链路浏览器验证通过）；v4.5.5（**平台外下游经销商报文式开放接口**：厂家 DMS ↔ 经销商自有 ERP 的 4 个 HMAC 机器凭证接口——入站经销商推采购订单/采购退货单、出站厂家发货回传出库/红字出库 webhook；Flyway V143–V144；已部署测试环境，入站 E2E 20/20 + 出站 E2E 13/13 + 铁律9 全过）；v4.5.4 = 同 SaaS 内跨租户**反向退货协同**（路径 C/D，无 Flyway，集成测试 17/17）；v4.5.3 = PC 工作台 UI 增强（面包屑 + 多菜单页签 Tags-View，纯前端）；v4.5.2 = GitHub Actions CI + 测试基线修复 12 例；v4.5.0 = 厂家进销存开关 + 厂家↔经销商跨租户订单协同（路径 A/B）；v4.5.1 = 协同自动化回归补齐；**生产环境最新已推送 v4.4.7（2026-08-30），v4.5.x 推送待用户明确指令**
- Flyway：**V121–V148**（**V148 = v4.6.3 审批流 SUBMITTER 节点 + 6 个一层审批模板播种**（CONTRACT_TERMINATE/AUTHORIZATION_TERMINATE/AUTHORIZATION_RENEW/PRODUCT_CREATE/DEALER_CREATE/SUPPLIER_CREATE，各 1 节点 ANY + SUBMITTER&ROLE 管理员，checksum=61545495，无表结构变更）；**V147 = v4.6.2 授权产品线化**（授权台账/终端/产品线重构，引入 order.authz.enforce 租户开关；部署时 checksum 对齐修复 1561521515→906988248）；**V146 = v4.6.1 定时邮件开关种子（system_settings scope='global' 插 3 个 mail.schedule.* 布尔配置，NOT EXISTS 幂等，无表结构变更）**；V145 = v4.5.6 经销商 button 权限补全（DO 块幂等复制模板租户 215 个 button 资源到缺 button 的 DEALER 租户 + 绑定 DEALER_ADMIN 策略 {view}，无表结构变更）；V140 = v4.5.0 跨租户协同；V141 = 演示数据迁移补回；V142 = collab 部分发货索引 ux→ix；**V143 = v4.5.5 外部经销商开放协同：open_app 加 partner_type/dealer_code/dealer_id/webhook_url/webhook_secret 5 列 + 建 open_partner_materials（物料对码）+ open_collab_messages（报文台账/幂等/重试）+ sales_outs 加 logistics_company/tracking_no + 种子 dealer EXT-D1 与 DEALER 类型 open_app**；**V144 = 物料映射种子修复（EXT-MAT-001→PRD-J001、EXT-MAT-002→PRD-J002、EXT-MAT-003→PRD-M001，NOT EXISTS 幂等）**；Current=148）
- 技术栈：Spring Boot 3.2 + Java 17 + JPA/Hibernate + Flyway + PostgreSQL 14 + Redis 7 + MinIO；Vue 3 + Vite 5 + Element Plus（PC）+ Vant 4（H5）+ Pinia；Docker Compose（backend/nginx/postgres/redis/minio 5 容器）；后端集成测试 zonky embedded-postgres 2.0.7（内嵌 PG 14.10）
- 测试环境：**http://dms-dev.mysolmed.com**（域名 → 43.128.145.141，裸域名 302→`/dms/`；IP 直连同样可用；ubuntu / Welcomeyyx0616；浏览器登录 sys_admin / Dms@123456，业务前台 admin / Sh123456，平台后台 admin / Sh123456）
- 生产环境：http://8.133.193.238/dms/

## 今日（2026-09-04）变更要点

- **v4.6.3（MINOR，审批流体系补齐 + 系统开关管理员页 + 首页/合同工作台 UI 紧凑化，Flyway V148，已部署测试环境并通过铁律9 浏览器逐项验证）**：对应用户 7 项需求（首页紧凑化 / 合同工作台筛选风格统一 / 合同终止 / 授权终止+续约 / 解耦开关迁至用户与权限管理员页 / 定时邮件开关管理员页 / 8 类业务审批流挂载）。完整条目见 CHANGELOG.md v4.6.3；要点：
  - **审批引擎 SUBMITTER 节点**：ApprovalAssigneeType 增 `SUBMITTER`（提交人本人）；ApprovalService.resolveNodeUsers 三分支——USER 按 config.refId、SUBMITTER 按 submitterId（null 跳过）、ROLE 经 userRoleRepository 展开，均 filter 同租户+active 去重；ApprovalTemplateService 与前端 AssigneePicker/FlowEditor/Templates/dict 同步支持。配合 ANY 或签，测试环境模板节点审批人 = 提交人本人 ∪ 系统管理员，一层即可。
  - **8 类业务审批挂载**：合同终止 CONTRACT_TERMINATE（已生效合同、ElMessageBox.prompt 终止原因必填）；授权终止/续约 AUTHORIZATION_TERMINATE / AUTHORIZATION_RENEW（续约为独立业务类型，复制经销商+产品线+终端+新有效期；**仅 active/not_started 状态可操作，后端状态门禁 + 前端按钮按行状态渲染**）；销售订单条件审批（成交价与基础价任何差异即审，**仅产品全局折扣+经销商全局折扣免审**）；销退单 RMA 一律审批；物料/经销商/供应商创建 pending_approval 待审不可选用、通过后生效。
  - **系统开关管理员页**：SystemSwitchController `/api/system-switches`（GET 聚合 4 开关：租户级 order.authz.enforce + 全局 mail.schedule.enabled/report/approval；POST 白名单分流，非法 key 40001），类级 `@perm.isTenantAdmin()`（id()!=null 守卫防匿名兜底误判）；旧 `/api/authorizations/order-enforce` 两端点已移除；前端新增 SystemSwitches.vue 挂「用户与权限」菜单，授权管理页摘除开关区块。
  - **Flyway V148**（checksum=61545495，无表结构变更）：幂等播种 6 个一层模板 ID28 合同终止 / 29 授权终止 / 30 授权续约 / 31 物料创建 / 32 经销商创建 / 33 供应商创建（各 1 节点 ANY + SUBMITTER&ROLE 管理员）；销售订单/销退/开票模板 ID25-27 既有启用；模板总数 33。
  - **UI 紧凑化**：Home.vue 快捷入口由大卡片改紧凑图标网格（需求①）；ContractWorkspace.vue 改 el-card + el-form inline size=small 单行筛选栏、操作按钮对齐其他菜单风格（需求②，201 行）。
  - **部署验证**：服务器侧 Maven/Node 构建（本机终端桥故障走 §14 绕路）；后端 jar 117,197,075 字节；前端 VITE_BASE=/dms/ 构建（index-BkwmA27h.js，198 assets，含 SystemSwitches chunk）；V147 checksum 对齐修复（1561521515→906988248，先核验迁移幂等已生效再对齐）后 V148 迁移成功 Current=148；铁律9 真实浏览器逐项过——系统开关页（挂钩=关闭、邮件 3 开关=开启）、审批模板 33 条、授权页门禁（终止/续约仅生效中行可见、无解耦开关区块）、合同工作台（紧凑筛选栏+终止按钮）、Console 无红错、H5 正常。
  - **踩坑**：前端部署须逐文件核验 index.html 引用的 hash chunk 实际落盘（rm -rf+cp 曾误判成功）；browser_wait_for 导航后易 IDE timeout（页面已加载直接快照）；browser_console_messages/network_requests 返回 object（content 数组）非数组不可直接 .filter。

## 2026-09-01 变更要点

- **v4.5.6（PATCH，经销商 button 权限补全 + 协同收货单号修复，Flyway V145）**：测试数据联调（2 个经销商租户各自下采购单 → 厂家销售订单 → 分别发货）中发现并修复两个缺陷。
  - **RBAC 缺陷（根因与修复）**：新开户经销商租户登录后业务菜单全部不可见、/dms/m/receipts 等业务路由 403。权限链 users→user_roles→roles→role_strategies→strategies→strategy_resources→resources；开户预置器 TenantRoleProvisioner 只建 `type='menu'` 资源（码为连字符 `:menu` 后缀），不建 `type='button'` 资源（码为下划线 `module:action`，如 purchase_order:view/receipt:view/sales_order:view），而前端菜单显隐 menuVisible() 与路由守卫检查的是 button 码；V61 历史补种只覆盖当时已存在租户。**V145**（backend/src/main/resources/db/migration/V145__dealer_button_permissions.sql）：DO $$ 块以模板经销商 DEALER_A1（22222222-0000-...-001）的 215 个 button 资源为基准，循环所有 tenant_type='DEALER' 且无 button 的租户，幂等复制 resources（按 tenant_id+code NOT EXISTS）并以 operations={view} 绑定其 DEALER_ADMIN 角色的管理员策略（strategy_resources 按 strategy_id+resource_id NOT EXISTS）。中期根治方向：TenantRoleProvisioner 开户直接预置 button（本版未做）。
  - **收货单号修复**：CrossTenantCollabService.onSalesOutShipped（路径B）原 `docNoGenerator.next("RK")` 与经销商 GR 序列撞段，改为正向 `next("GR")`（L269）、红字 onRedSalesOutShipped `next("RGR")`（L511）；修复前历史单 RK-20260901-00001 为数据残留不影响功能。
  - **关键数据/登录**：D1=DEALER_D1（22222222-1111-...，user 163 dealer_d1_admin / dealerId 19 / 收货仓 29 / role 160→strategy 76）、D2=DEALER_D2 苏州康宁（a86dae80-...，user 164 dealer_d2_admin / dealerId 2 / 收货仓 28 / role 161→strategy 77）；登录 POST /api/auth/login body `{"tenantCode":"DEALER_D1","username":"dealer_d1_admin","password":"Sh123456"}`；GET /api/me/permissions 的 data 直接是权限码字符串数组（非对象）。
  - **验证**：flyway_schema_history 145 success；D1/D2 各 215 button+21 menu、/api/me/permissions 243 码含 7 个关键菜单码；真实浏览器两租户菜单全可见，收货页见 GR-20260901-00003（D2 待处理）/ RK-20260901-00001（D1 待处理），采购页见厂家销售单号 SO-20260901-00005/00004 回写。
  - 最新踩坑点：**①注入凭据后必须先导航 /dms/home 触发 fetchPermissions 缓存（localStorage dms:user:permissions），再整页进 /dms/m/* 业务路由，否则路由守卫在权限返回前执行撞 403；②403 错误页无 main 元素，browser_snapshot 不能带 selector="main"；③paramiko 执行 shell 含 `date +%Y%m%d` 的命令在 Python % 格式化串里会报 ValueError，须用 .format() 拼接。**
- **v4.5.5（MINOR，平台外下游经销商报文式开放接口，Flyway V143–V144）**：面向**不在 DMS 平台内、自有 ERP 系统的下游经销商**，提供厂家 DMS ↔ 经销商系统之间的单据报文协同（与"同 SaaS 内跨租户协同"com.dms.collab 是两套独立能力；与厂家 ERP 对接 /open/api/erp 亦无关）。
  - **4 个接口（docs/03_接口文档/跨租户订单协同接口文档_v4.5.5.docx）**：①入站 `POST /open/api/collab/purchase-orders/submit`——经销商推采购订单（报文 header 带 poNo/dealerCode，lines 带 externalCode/qty），DMS 建 DRAFT 销售订单；②出站 **SHIP_NOTICE webhook**——厂家发货确认后 DMS 主动 POST 经销商 webhook_url，回传出库单号/发货日期/物流/行明细；③入站 `POST /open/api/collab/purchase-returns/submit`——经销商推采购退货单（is_red 链路），DMS 建 DRAFT 红字销退单；④出站 **RED_SHIP_NOTICE webhook**——红字出库后回传红字出库信息。入站幂等：open_collab_messages 唯一索引 ux_ocm_in_idem (app_id,msg_type,partner_doc_no) WHERE direction='IN'，重复报文返回 IDEMPOTENT 不重复建单。
  - **新增代码包 `backend/src/main/java/com/dms/openapi/`**：`OpenApiAuthFilter`（拦截 /open/api/**，HMAC-SHA256 四头 X-App-Key/X-Timestamp(毫秒)/X-Nonce/X-Signature，signString=METHOD大写\nREQUEST_URI\ntimestamp\nnonce\nsha256Hex(body)，时钟偏移 ±5 分钟，失败返回 ApiResponse 业务码：HTTP 401=40501、403=40403）；`OpenCollabController`（接口1/3 入站端点）；`service/ExternalCollabOpenService`（入站建单：物料对码 open_partner_materials、dealerCode 校验 40301、建 DRAFT 单 tax_rate=0.13/unitPrice 缺省 0、collab 版 lookupWarehouse 查不到静默 null）；`service/ExternalCollabWebhookService`（出站：发货事务内登记 open_collab_messages(OUT,PENDING) + afterCommit 异步推送，**推送失败绝不阻断发货**；成功判定 isBusinessOk = HTTP 2xx 且（body 空/无 code/code=0）；失败退避 5/15/30/60/120/360/720/1440 分钟、MAX_RETRY=8 后 next_retry_at=NULL 停止；物料映射缺失 markRetryable 固定 30 分钟重试不计数；签名 secret 用 webhook_secret，空则回退 app_secret）；`dto/collab/`（CollabPurchaseOrderRequest/CollabPurchaseReturnRequest/CollabSubmitResult，统一 ApiResponse 壳 code=0 成功）。
  - **DDL（V143）**：open_app 加 5 列（partner_type 默认 'ERP'、dealer_code、dealer_id、webhook_url、webhook_secret）；新建 open_partner_materials（租户/app/经销商/外部物料编码↔产品对码，ux_opm_ext_code 唯一 (app_id,external_code) WHERE deleted_at IS NULL）；新建 open_collab_messages（direction IN/OUT、msg_type PURCHASE_ORDER/SHIP_NOTICE/PURCHASE_RETURN/RED_SHIP_NOTICE、partner_doc_no/local_doc_no、line_refs JSONB、request_body/response_body、status、http_status、retry_count、next_retry_at、idx_ocm_out 重试扫描索引）；sales_outs 加 logistics_company/tracking_no；种子 dealer EXT-D1 + DEALER 类型 open_app（AppKey=`dms-ext-dealer-d1`、AppSecret=`ext-dealer-d1-secret-20260901`、WebhookSecret=`ext-dealer-d1-whsec-20260901`、webhook 占位 http://127.0.0.1:9999/webhook/collab、system='DEALER_ERP'）。V144 修复物料映射种子（V143 误用 PROD-000001..003，V144 补种真实编码 EXT-MAT-001→PRD-J001、EXT-MAT-002→PRD-J002、EXT-MAT-003→PRD-M001）。
  - **验证**：入站 E2E `automation_test/v454_open_collab_e2e.py` **20/20 通过**（HMAC 签名正确/错误签名 40501/时间戳过期/nonce 重放/幂等重复/物料未对码 40006/dealerCode 不匹配 40301 等）；出站 E2E `automation_test/v455_open_collab_outbound_e2e.py` **13/13 通过**（本地 webhook 接收端、发货触发 SHIP_NOTICE、红字出库触发 RED_SHIP_NOTICE、接收端非 2xx 退避重试、对端返回业务错误码标记 FAILED）；铁律9 真实浏览器终验 8 个用户入口 URL 全过；已部署测试环境。
  - **文档**：Word 接口规范 `docs/03_接口文档/跨租户订单协同接口文档_v4.5.5.docx`（报文式：URL 不含单据 id、单据号在报文 header、header+lines 结构、日期仅 yyyy-MM-dd、含 HMAC 鉴权章节与示例报文）；版本号说明：本特性开发期曾用 v4.5.4 标号，因 CHANGELOG 中 v4.5.4（2026-08-31）已分配给同 SaaS 内反向退货协同，正式顺延 **v4.5.5（2026-09-01）**。
- **v4.5.4（同 SaaS 内跨租户反向退货协同，路径 C/D，无 Flyway）**：com.dms.collab.CrossTenantCollabService 增 onPurchaseReturnSubmitted（经销商采退提交→厂家红字销退单，link_type=PR_TO_RED_SALES_ORDER）与 onRedSalesOutShipped（厂家红字出库→经销商红字入库，RED_OUT_TO_RED_RECEIPT）；钩子挂 PurchaseReturnController.submit 与 SalesOutBatchService.confirmBatch；CrossTenantCollabIntegrationTest 17/17；详见 CHANGELOG v4.5.4 与 docs/03_接口文档/cross-tenant-collab-api.md。

## 2026-08-31 变更要点

- **v4.5.3（PC 工作台 UI 增强：面包屑 + 多菜单页签 Tags-View，纯前端）**：参考主流后台系统，在内容区顶部新增两大导航能力，用户无需反复打开左侧菜单即可定位与切换页面。
  - **新增文件**：①`frontend-vue/src/utils/pageMeta.js`——页面元信息解析器：由 `config/menu.js` 的 MENU_GROUPS 扁平化出 FLAT_MENU，`resolvePageMeta(route)` 返回 `{title, isMenu, crumbs}`（/home 特判单面包屑「首页」→ fullPath/path 精确匹配 → 前缀匹配 → 27 条 DETAIL_PARENT 正则映射详情页父菜单 → 兜底）；`tagKeyOf(route)` 菜单页用 `route.fullPath`（保留报表 query 区分），非菜单页用 `'name:'+route.name`。②`frontend-vue/src/store/tags.js`——Pinia options store：页签状态 `{tags, activeKey, reloading}`，**sessionStorage key `dms:tags-view` 持久化**（F5 后恢复），首页页签 `affix:true` 固定不可关；actions：addRoute（跳过 public/print/mobile 路由）、closeTag（返回应跳转 fullPath）、closeOthers、closeAll（回 /home）、setActive、refreshSignal/refreshDone、reset（退出登录清场）。③`frontend-vue/src/layout/Breadcrumb.vue`——el-breadcrumb（separator="/"）+ transition-group，中间层级为可点击链接、末级为当前页纯文本。④`frontend-vue/src/layout/TagsBar.vue`——页签 chip 栏（el-scrollbar 横向滚动 + 激活页签自动滚动居中），chip 支持点击切换、×关闭、鼠标中键关闭、@contextmenu 右键菜单（刷新页面/关闭页签/关闭其他/关闭全部，固定首页的"关闭页签"置灰）；样式全用 DMS CSS 变量（--dms-color-primary / --dms-bg-container 等），深色模式 `:global(html[data-mode='dark'])` 覆盖。
  - **改动文件**：`frontend-vue/src/layout/index.vue`——顶栏 `<Breadcrumb />` 替换原 page-title；`</el-header>` 与 `<el-main>` 之间挂 `<TagsBar />`；router-view 用 `<template v-if="!tagsStore.reloading">` 包裹 + `<keep-alive :max="12">`（meta.noCache 除外）实现页签缓存与"刷新页面"销毁重建（watch reloading → 60ms 后 refreshDone）；退出登录时 tagsStore.reset()；删除无用 nextTick 导入与旧 currentTitle/.page-title。
  - **构建与部署**：业务前端必须 **`VITE_BASE=/dms/`** 构建（vite.config.js `base: process.env.VITE_BASE || '/'`，默认 / 会致资源路径与服务器 /dms/ 不符）；产物 index.html 引用 `/dms/assets/index-*.js`。测试服 nginx 容器（dms-test-nginx）bind-mount `./frontend:/usr/share/nginx/html:ro`，业务前端在 `frontend/dms/`、独立 admin-vue 在 `frontend/dms/admin/`（本轮不动）、根 / 为宣传页；**前端纯静态部署 = 解压 dist 到 dms/（保留 admin）→ chown root:root → `docker exec dms-test-nginx nginx -t && nginx -s reload`，无需重建镜像/force-recreate**；部署前备份 frontend 到 /opt/dms/backups/。
  - **验证**：本地 vitest 36/36 + vite build 成功；铁律9 真实浏览器（sys_admin 厂商视角）逐项过——面包屑（首页 / 首页·基础数据·产品管理 / 详情页"首页·基础数据·经销商管理·经销商详情"父级可点击）；页签新增/点击切换/×关闭非当前页/×关闭当前页自动跳相邻/右键菜单四项/关闭其他/关闭全部只剩固定首页/F5 后 sessionStorage 页签完整恢复/深色模式栏底边框与文字对比度正常；面包屑"首页"链接点击回跳；Console 无 error（仅 1 条与本次无关的 el-radio 弃用 warn）；必检 URL 全过（/ 302→/dms/home、/dms/、/dms/home、/dms/admin/→后台登录、/dms/mobile/login、/dms/mobile/register 均 200）。
  - 最新踩坑点：第 58–61 条（本地无系统 node 用便携版 Node 路径；VITE_BASE=/dms/ 构建；前端静态部署 nginx reload 定式；菜单路由 key 须与 menu.js 一致——/m/product-categories 是错误 key，正确为 /m/categories，错误 key 会兜底显示"业务模块"；非语义 div 组件（页签 chip）AX 快照无 interactive ref，浏览器自动化须用 browser_evaluate 原生事件驱动——click/close dispatchEvent/contextmenu 带 clientX/Y，getComputedStyle 验深色、边框读对应方位）。
- **v4.5.2（接入 GitHub Actions CI 流水线 + 全量测试基线修复）**：①新建 `.github/workflows/ci.yml`——push/PR 到 main 触发，双并行 job：`backend-test`（ubuntu + JDK17 temurin + maven 缓存，backend 目录 `mvn -B verify`，失败也上传 surefire-reports）、`frontend-build`（Node 20 + npm 缓存，`npm ci` → vitest 单测 → vite build，失败也上传 dist）；CI 无需 Docker/PG/Redis（内嵌 PG14 + Redis Mock）。②全量 `mvn test` 暴露并修复 12 例基线失败：11 例旧 Controller 集成测试造用户未授权致 403（补 `grantPermissions` 显式权限码；注意 `"*"` 是字面码非通配、`isNotIn(404,5xx)` 断言不含 403 会假性通过）+ 1 例 V4CalculatorTest 百分比折数造数过时（v4.4.7 折数语义下填 20=减80%，改填 80=减20%=折扣额 200）。③验证：后端 **Tests run 147, Failures 0, Errors 0**；前端经测试服务器（node v20.20.2，与 CI 等价）实测 npm ci 299 包、vitest **36/36**、vite build 成功（28.51s）。④**CI 激活需 git commit + push 工作流文件到 GitHub**（vinkinyyx/dms-demo main）；本次仅测试代码 + CI 配置，无 Flyway/接口/生产逻辑变更，未部署任何环境。最新踩坑点：第 55–57 条。
- **v4.5.1（跨租户协同自动化回归补齐 + 缺陷修复）**：v4.5.0 首版仅手工测试，本次补齐双层自动化并修 5 项缺陷。①**端到端脚本** `automation_test/v450_collab.py`（接口调用 + DB 回读，TAG 标记种子数据并自清理，覆盖 10 个缺口：路径A/B 正常链路、重复幂等、未对码/未绑定阻断、分批累计、路径A台账复用、未绑定静默跳过）**56/56 通过**；②**后端集成测试** `CrossTenantCollabIntegrationTest`（zonky 内嵌 PG14 + Flyway 全量，TenantContext 切双租户直调 CrossTenantCollabService）路径A 5 例 + 路径B 6 例 **11/11 通过**，既有 Inventory 7/Order 7/SalesReturn 2/Chain 4 共 20 例回归全绿，**合计 31/31**；③修复：二次部分发货整笔跳过（改 outLineId 行级幂等）、分批重复补 PO（linkRowBySalesOrder/linkRowBySalesOut 先复用 + upsertPoLineQty 累计 5+3=8）、purchase_order_lines 无效列 updated_at、收货单缺 dealer_id/warehouse_id 幽灵库存（自动补建 DEALER-SELF 主体 + COLLAB-DEFAULT-WH 默认仓）、测试基建撞 V136 NOT NULL（工厂 + 6 处 Dealer.builder() 补 4 字段）；④Flyway V141/V142。详见 CHANGELOG v4.5.1 与测试报告 §8。
- 最新踩坑点：第 52–54 条（**Flyway 新增 NOT NULL 列后 Hibernate 显式插 null 不走 DEFAULT，所有 builder 造数点必须显式赋值**；跨租户联动幂等键必须行级、补单先复用后新建、下游单据外键带全防幽灵库存；embedded-postgres 集成测试范式与 surefire 报告读取）。

## 2026-08-28 变更要点

- **v4.4.2（品牌与域名）**：①全站替换 MySolMed 品牌 logo（藏青 #0B2545「m」标 + 青色圆点）——frontend-vue 与 admin-vue 的 `DmsLogo.vue` 改为 `<img>` 方案（`src/assets/brand/logo-mark.png` / `logo-mark-white.png`，inverse 白标用于深色侧边栏/登录页），两项目 index.html 增加 favicon-16/32/48 + apple-touch-icon、theme-color 改 #0B2545、标题改「MySolMed DMS …」；覆盖 PC 登录页/工作台首页/侧边栏、移动 H5 登录页、平台后台登录页与布局共 7 个使用点。②测试环境启用域名 `dms-dev.mysolmed.com`（DNS 已解析到 43.128.145.141；nginx `server_name` 早已含该域名）；根路径 `/` 由"返回宣传手册 landing"改为 **302 → `/dms/`**（测试环境直达 DMS），宣传手册保留在 `/brochure/`；后端 `APP_BASE_URL` 改为 `http://dms-dev.mysolmed.com/dms`（审批邮件链接）。③**nginx 变更管控规则落档**（见 AGENTS.md）：禁随意调整，改动须备份→最小 diff→`nginx -t`→reload/重启→容器内 `nginx -T` 取证；注意 **bind-mount 下 `sed -i` 换 inode 后必须 restart 容器**（reload 不生效，本次实测踩坑）。
- 验证：铁律 9 真实浏览器全过——裸域名 → 302 → `/dms/home` 工作台；PC 首页 2 logo、移动 H5 登录页 1 logo、后台 1 logo 均 `naturalWidth=512`；`/api/auth/login` 200；全入口 curl 200；Console 无 error。

- **v4.4.0（R1–R7，MINOR 寄售业务闭环）**：寄售补货/开票/红冲订单类型与寄售开关、寄售台账 consignment_stock + movements 五类流水（REPLENISH_IN/INVOICE_LOCK/INVOICE_DEDUCT/INVOICE_RELEASE/REPLENISH_OUT）、经销商资信模块 dealer_credit_profiles、进销存报表精细化、资源选择器；Flyway V135–V137（含 RMA/INVOICE_ORDER 审批模板）。
- **v4.4.1（PATCH，BUG-01~04 + 红字补货 + 拣选交互方案 A）**：①BUG-01 补货→寄售入库链路三缺口修复（红字补货建单 SOR/validateReplenishRed、ux_sales_serial 改部分唯一索引豁免红字行、SalesOutService 钩子拼写 `"REPLENISH"`→`"REPLENISHMENT"`）；②BUG-02 供应商门户菜单 inventoryOnly 补全；③BUG-03 CrudView 浮动 Promise 补 catch；④BUG-04 官网 landing 图片 404 修复；⑤开票寄售库存拣选弹窗（OrderCreate.vue：经销商 ResourcePicker 门禁、整行勾选、序列号限 1、实时汇总、整单替换、el-tag 回显）；Flyway V138–V139。
- 验证：E2E scripts/e2e_invoice_consignment.py **23/23 全绿**（开票闭环 4 场景 + 红字补货 9 项检查）；铁律 9 真实浏览器门禁全 PASS（/、/brochure/、/dms/、登录→工作台、寄售台账列表、H5 供应商、订单新增→拣选弹窗全链路，弹窗实测勾选 B2608-B2 应付 ¥470.40）；测试库存 seed_consignment.py 9 行台账。
- 最新踩坑点：第 48–51 条（红冲共用序列号须改部分唯一索引且所有 INSERT 绑定 is_red、字符串字面量分支须与枚举逐字核对、红单重建行后回调须重取行 ID、integrated_code_mode Exec V8 沙箱范式）。

## 新会话加载顺序

1. 本文件 → `docs/AI开发文档.md`（完整上下文）
2. `AGENTS.md` + `.trae/rules/project_rules.md`（铁律与部署规则）
3. `docs/项目设计文档.md`（架构与版本变更记录）
4. `docs/文档索引.md`（全部文档导航）
