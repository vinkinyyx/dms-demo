# AGENTS.md - DMS 项目工作准则

> 本文件是 Codex 在本项目中的**强制工作准则**，每次会话自动加载。
> 详细规范见 `.memory/layers/`，本文件只规定**必须执行的流程和不可跳过的检查**。
> 最后更新：2026-08-27（Gap5升级为project_rules.md【铁律9】：部署后首检必须逐条验证文档中所有用户入口URL，含 /dms/ 路径，禁止只 curl 根路径）

---

## 1. 项目概览

- **产品**：DMS 经销商管理系统（PC 业务前台 + 平台后台 + 移动 H5）
- **后端**：Spring Boot 3.2 + Java 17 + MyBatis-Plus + Flyway + PostgreSQL 14 + Redis 7
- **前端**：Vue 3 + Vite 5 + Element Plus（PC）+ Vant 4（H5）+ Pinia
- **部署**：Docker Compose + Nginx
- **测试环境**：http://43.128.145.141/dms/（后台 http://43.128.145.141/dms/admin/，移动端 http://43.128.145.141/dms/mobile/login；API 与健康检查仍走根路径 /api、/auth、/actuator）
  - ⚠️ **当前部署状态（2026-08-27）**：前端 vite.config.js `base=/`（未设置 VITE_BASE=/dms/），为兼容文档链接，Nginx 已对 `/dms/*`、`/dms/admin/*`、`/dms/mobile/*` 设置 302 外部重定向到对应根路径；**访问 `/dms/` 后浏览器地址栏会变为 `/`**，这是预期行为，不是错误
  - ⚠️ **铁律9强制**：每次部署后必须用真实浏览器逐个验证上述 3 个 `/dms/*` 入口 URL，**不允许只验证根路径**
- **生产环境**：http://8.133.193.238/dms/ （未经用户明确指令，禁止操作）
- **演示账号**：租户 `default` / `admin` / `Sh123456`

---

## 2. 需求处理三段式（强制执行）

### 阶段 A：理解与确认（动手前必须完成）

收到任何非平凡需求后，**禁止直接编码**。必须先输出一份简短的需求确认，包含：

1. **需求拆解**：逐条列出用户要什么，每条编号
2. **影响范围**：涉及哪些文件 / 模块 / API（给出路径）
3. **不做什么**：明确边界，防止过度修改
4. **验收标准**：怎么算做完了（可验证的条件）
5. **关键假设**：不确定的地方，写明默认假设

输出后，如果用户有回应，按用户意见修正后再执行；如果用户说「开始/继续/没问题」，立即进入阶段 B 全自动执行，不再追问。

### 阶段 B：执行

- 严格按 `.memory/requirement-closure.md` 的需求执行闭环：逐条编号登记(R1/R2...)、逐条落地、逐条加可执行验证、逐条回读、最终逐条汇报，杜绝「提到的问题没执行」。
- 用 TodoWrite 跟踪每条需求的进度
- 严格遵守第 3 节的 UI 一致性规则
- 修改完成后先本地验证，再部署测试环境
- 遇到报错自行修复（最多 3 轮），不要把错误抛给用户

### 阶段 C：交付自检（不通过不准交付）

交付前必须逐项完成第 4 节的检查清单，在最终汇报中明确说明每项的结果。

---

## 3. UI 一致性规则（高频违反点）

> 完整规范见 `.memory/layers/layer2-conventions.md` 第十八章和第二十六章。

### 3.1 列表页（所有业务列表页必须遵守）

- 使用统一组件 `frontend-vue/src/components/CrudView.vue`，**不要自己写列表布局**
- 搜索区：查询、重置按钮**始终显示**，不可隐藏
- 顶部工具栏从左到右固定顺序：`查询 -> 重置 -> [spacer] -> 导入 -> 导出 -> 新增 -> 业务按钮`
- 行内按钮：可见数 <=1 时平铺；>1 时只平铺第 1 个，其余收进「更多」下拉
- 第 1 个行内按钮必须是「查看/详情」等低风险动作
- 删除/驳回等危险操作放「更多」下拉中，使用 danger 样式 + 二次确认
- 所有按钮必须有 `permissionCode`，通过 `v-has` 指令控制显隐
- 表格：`border stripe size=small`，操作列 `fixed=right`
- 分页：`page-sizes=[20,50,100]`，右对齐

### 3.2 按钮显隐规则

- 无权限的按钮**不渲染、不占位**（用 `v-has`，不是 `v-if=false`）
- 状态相关按钮（提交/审批/驳回/取消）必须按行数据状态过滤，不可用时不显示
- 未实现的功能按钮**不允许显示**（不要放占位按钮）

### 3.3 视觉风格

- 企业级沉稳风格：不要过度圆角、不要花哨配色
- 左侧菜单底色不要太深
- 日期时间统一格式：日期 `YYYY-MM-DD`，日期时间 `YYYY-MM-DD HH:mm:ss`，使用统一格式化工具
- 中文标签禁止出现 `?`、`????` 乱码
- 长文本（报文/日志/堆栈）必须提供一键复制按钮
- PC 端和移动端改动**分开提交**，不要混在一个修改里

---

## 4. 交付自检清单（逐项执行，逐项汇报）

### 4.1 代码级检查

- [ ] 没有硬编码的 API 路径，使用 `frontend-vue/src/api/` 下的封装
- [ ] 没有直接渲染 ISO 日期字符串或 UTC 时间（搜索 `T00:00:00`、`.toISOString`、`+08:00`）
- [ ] 没有中文乱码（搜索 `????` 替换字符）
- [ ] 没有 `console.log` / `debugger` 遗留在生产代码中
- [ ] 新增按钮都有 `permissionCode` 并用 `v-has` 控制
- [ ] 危险操作有二次确认弹窗

### 4.2 功能验证（必须用浏览器实际点击，不能只看代码）

- [ ] **【铁律9 · 部署后首检 GATE】（最高优先级，必须第一个做，禁止跳过）**：对照 AGENTS.md §1 和 project_rules.md 环境信息中的**全部用户入口 URL 清单**，用 TRAE-browseruse 真实浏览器**逐条打开**，每条都必须验证：
  - 测试环境必检 ≥ 5 条：`/`（根）、`/dms/`、`/dms/admin/`、`/dms/mobile/login`、`/actuator/health`
  - 生产环境必检同结构（用户明确指令时才测）
  - 通过标准：HTTP 200 或 302 后最终 200；DOM refs ≥ 20（非空骨架）；Console 无红色 error；Network 无 5xx
  - **特别提醒**：若访问 `/dms/` 最终跳 `/error/404`，先 `localStorage.clear()` + 重新登录排除旧会话假 404，再判断是否是真的 VITE_BASE 与 URL 前缀不匹配
  - 不通过 = 部署失败，**绝对不允许**用「容器是 Running、健康检查是 UP、API 登录是 200」就宣称部署通过
- [ ] 受影响的页面能正常打开，无白屏、无 404
- [ ] 页面加载后**浏览器 Console 没有红色错误**
- [ ] 页面加载后**网络请求没有 500/404**（预期内的除外）
- [ ] 每个按钮点击后有正确响应（弹窗打开 / 请求发出 / 提示出现）
- [ ] 表单提交、弹窗打开关闭正常
- [ ] 列表搜索、重置、分页正常
- [ ] 本次修改的需求逐条验证通过
- [ ] 审批流完整生命周期验证：创建→提交→审批通过 **AND** 审批拒绝/退回/撤回→关联资源回滚检查（代金券、库存锁定、分摊金额）

### 4.3 回归验证

- [ ] 运行深度冒烟测试：`node tools/smoke-test.cjs`
- [ ] 冒烟测试覆盖了受影响模块的核心页面
- [ ] 没有破坏未修改的相邻功能

---

## 5. 测试要求（按版本分层，杜绝浮于表面）

> 完整策略见 docs/02_设计/test-strategy.md。本节是项目内必须遵守的契约。

### 5.0 测试分层（按发布版本）

| 版本 | 触发 | 工具 | 范围 |
|------|------|------|------|
| PATCH (4.2.x -> 4.2.y) | node tools/test-scope.cjs | 范围深度测试（5 维） | 改动模块 + 上下游 + 5 条核心流基线 |
| MINOR (4.2 -> 4.3) | node tools/smoke-test.cjs | 全量冒烟（保持现状） | PC + Admin + Mobile 全页面 |
| MAJOR (4.x -> 5.0) | MINOR 全部 + 兼容性 + 性能 | 全量 + 升级验证 | 同上 + 数据迁移 + 性能基准 |

### 5.0.1 手动 scope 契约（PATCH 强制）

- 每次 PATCH 修补前，Codex 必须像对待新需求一样向用户确认涉及的业务流与上下游（按 §2 阶段 A 规则）。
- 用户不明确说这次改了什么模块时，禁止自动推断或基于 git diff 猜测，必须反问。
- 反问模板见 docs/02_设计/test-strategy.md §4.2。
- 范围识别靠 tools/scope-map.json（5 条核心流 + 13 模块映射），新增/删除模块时同步更新。

### 5.0.2 5 条核心业务流基线

PATCH 改动任何模块时，默认必须把以下 5 条核心流也跑一遍（除非用户明确说这次改动不影响 F1-F5）：
- F1 下单 -> 审批 -> 出库 -> 库存扣减
- F2 销退 -> 选发货单 -> 可退量锁定 -> 回写
- F3 采购 -> 收货 -> 入库
- F4 BOM + 促销 + 下单计价
- F5 审批流：提交 -> 通过/驳回 -> 状态

### 5.0.3 5 维测试结构

对每个被测流程，必须覆盖 5 维：前端 UI 真实点击 / 后端 API 直调 / 数据库 SQL 回读 / 业务规则（BOM/促销/价格/库存/反向顺序）/ 异常路径（撤销/驳回/重复/并发）。任一维度缺失，PATCH 不可发布。

### 5.1 什么不算测试

- 只访问页面 URL 看是否返回 200
- 只看代码判断「应该没问题」
- 只验证登录成功就认为全部通过
- 构建成功不等于功能正常
- **部署后只调健康检查 `/actuator/health`，不打开浏览器访问前端真实页面**（Nginx 路径错、部署路径串、前端静态资源丢，后端依然 healthy，但用户看到的是 500/白屏）
- **只调 API 层，不在浏览器验证列表列显示内容**（API 返回 dealerId=5 正常，但前端可能直接把数字 ID 渲染给用户）
- **只测审批通过，不测审批拒绝/退回/撤回后的资源回滚**（代金券 USED 不返还、库存锁定不释放，属于功能损坏但 API 全 200）

### 5.2 什么才算测试

- **打开真实浏览器，实际点击按钮**，观察弹窗/跳转/提示
- **监听 Console**：页面操作过程中不能有未捕获的 JS 错误
- **监听网络请求**：API 不能返回 500，不能有非预期的 404/4xx
- **验证可见内容**：表格有数据、标题正确、按钮文字正确
- **覆盖完整业务链**：新增 -> 填写 -> 提交 -> 列表刷新 -> 查看详情 -> 编辑 -> 删除/作废
- **必须验证提交结果**：不能只打开弹窗；必须点击保存/提交，等待接口成功，再回读列表/详情或数据库状态
- **覆盖反向交互顺序**：同一表单至少验证 A 先选、B 后选，以及 B 先选、A 后选两种路径
- **覆盖有状态业务规则**：数量、价格、库存、可退数量等必须用 API 或页面回读验证真实剩余值
- **验证特殊数据形态**：BOM/组合品、单品、已有有效价、已失效价、空数据、重复提交都要考虑
- **引用字段必须显示业务名称**：只读页/编辑页/明细表格中，任何外键字段（*Id）必须展示 xxxCode/xxxName，select/枚举必须展示中文 label，绝不允许把数字 id 或 EVERY_N/SKU 这类原始码值直接抛给用户
- **状态按钮必须验证接口与回读**：启用/停用/提交/审批等行内动作，必须先确认后端 mapping 存在（@PostMapping 等），点击后再 GET 详情或查库回读状态变化；禁止前端空 catch 静默吞错
- **价格/库存解析必须前后端一致**：前端加载价格/库存时，参数（如 partnerId）与回退优先级必须与后端 Service 完全一致；后端抛错必须带业务编码+名称（如「产品 [PRD-B001 ...] 没有维护有效销售价格」），禁止只抛 id
- **审批/工作流提交必须验证最终状态**：提交后必须回读 status 与 approval_instances，确认进入 PENDING_APPROVAL/RUNNING，不能只看接口 200；部署后必须校验 approval_node_assignees 非空
- **测试数据前后置清理**：自动化脚本创建的价格、单据等数据必须在脚本开始和结束时清理，避免脏数据导致假通过/假失败
- 截图保存证据到 `automation_test/v4-browser-results/`

### 5.3 历史教训：这些情况不算测过

- 只访问页面 URL 或只打开新增弹窗，没有填写和保存
- 只监听 500，不监听 404/4xx，导致错误跳转和参数错误漏测
- 只按一种顺序操作选择器，遗漏选择顺序导致的请求参数丢失
- 只检查页面提示，不回读可退数量/价格状态等业务结果
- 只测普通单品，不测 BOM 母件/子件逐行维护
- 详情/编辑页里把 targetProductId=13、cycle=EVERY_N 这种 id/枚举码直接显示给用户，没有渲染成产品名/中文 label
- 行内按钮（启用/停用/提交）只点了一次正向路径，没有验证后端接口是否存在、是否 404 被 catch 吞掉，也没有 GET 回读状态
- 前端显示的价格/库存与后端解析规则不一致（没传 partnerId、回退到任意 active 行），导致 UI 看起来有价格、提交却报「产品 X 没价格」
- 提交后只看接口 200 就认为成功，不回读 status/approval_instances，导致单据静默回退草稿而漏测
- 测试脚本本身语法错误或没有接入 `npm run test:all`，导致以为跑了实际没跑
- **v4.3.0 新增教训**：
  - 审批拒绝/退回/撤回后，关联资源（代金券、库存锁定等）必须回滚，不能只更新单据状态
  - 代金券分摊金额总和不能超过面值，每行扣减金额必须按比例分摊
  - 所有列表/详情/编辑页中的外键字段（dealerId/productId）必须显示编码+名称，禁止直接显示数字 ID
  - 销退订单必须强制先选经销商再选出库单，一个销退只能针对一个客户
  - 错误消息中禁止只抛产品 ID（如"产品 [682] 没有维护有效销售价格"），必须带产品编码+名称
  - **部署后必须打开真实浏览器访问前端**：不能只看 docker ps 或 /actuator/health 返回 UP 就算部署成功；必须访问用户实际打开的 URL（根路径 /dms/ 或 /），等待页面完全渲染，验证工作台首页、菜单展开、核心列表页都能加载，否则用户遇到 500/白屏就是测试失职

### 5.3.1 v4.3.0 测试 Gap 反思与补强（强制执行）

**背景**：v4.3.0 版本测试中，AI 报告"14/14 全通过"，但用户实际操作发现至少 5 个严重 Bug。根因是测试方法存在系统性缺陷。

**Gap 1：只做 API 层测试，不做浏览器真实操作**
- 表现：API 返回 200 就认为功能正常，但前端显示 ID 而非名称、交互流程顺序错误等问题无法发现
- 补强：所有涉及 UI 的功能，必须用 TRAE-browseruse 或 Playwright 真实点击验证，不能只调 API
- 检查项：①列表页所有列显示的是编码/名称而非 ID ②下拉选择器显示中文 label ③错误消息包含业务编码+名称

**Gap 2：没有测试状态回滚**
- 表现：审批拒绝后代金券状态未回滚，但因为只测试了"提交成功"，没有测试"审批拒绝"路径
- 补强：所有涉及审批流的功能，必须测试完整生命周期：创建→提交→审批通过 AND 审批拒绝/退回/撤回→验证关联资源回滚
- 检查项：①审批拒绝后代金券状态恢复为 ISSUED ②审批退回后库存锁定释放 ③审批撤回后单据回草稿

**Gap 3：没有测试金额计算正确性**
- 表现：代金券面值 200 元但扣减 500 元，因为只验证了"接口返回 200"，没有验证金额数值
- 补强：所有涉及金额计算的场景，必须用 API 或数据库回读验证具体数值，不能只看接口成功
- 检查项：①代金券扣减总和 ≤ 面值 ②折扣后金额 = 原价 - 折扣 - 代金券扣减 ③分摊金额按行比例正确

**Gap 4：没有测试交互顺序约束**
- 表现：销退订单可以先选出库单再选客户，但业务规则要求必须先选客户
- 补强：所有有选择器依赖关系的表单，必须测试"先 B 后 A"的反向顺序，验证前端是否有前置校验
- 检查项：①销退必须先选经销商才能选出库单 ②订单必须先选产品才能加载价格 ③选择器有级联依赖时不能跳级

**Gap 5：部署后不验证前端真实可访问，只看健康检查（⚠️ 2026-08-27 已升级为【project_rules.md 铁律9 + 防回归规则七】，现为强制执行 GATE，不再是建议性 Gap）**
- 表现：2026-08-27 用户反馈测试环境直接报 500，但 AI 之前仅通过后端/actuator 判断部署完成；Docker 容器是 Running、后端健康是 UP，但用户实际访问的 URL 因为前端部署路径、Nginx root 不匹配等原因返回 500，属于部署成功但实际不可用
- 本次真实根因组合：① Nginx alias `/dms/` → `/usr/share/nginx/html/dms/` 但文件没放 dms 子目录（500）；② 第一次修用内部 rewrite 导致 Vue Router（base=/）收到 `/dms/` 跳 `/error/404`；③ 最终修法：`/dms/*` 302 外部重定向到 `/*` 才真正解决
- 补强（现为强制铁律，违反=部署失败）：每次部署完成后的**第一个检查步骤**必须是「用 TRAE-browseruse 打开 AGENTS.md 和 project_rules.md 中列出的**所有**用户入口 URL（`/`、`/dms/`、`/dms/admin/`、`/dms/mobile/login`、`/actuator/health`），等待页面渲染完毕，确认不是 500/404/白屏，然后完成一次真实浏览器登录 → 进入首页 → 展开2个菜单 → 进入 1~2 个核心列表页确认数据能加载」；禁止把健康检查、容器状态、API 登录成功当作前端部署验证通过的证据
- 检查项：①每个 URL HTTP 200 或 302 后最终 200 ② 工作台首页 DOM refs ≥ 20（非空骨架） ③ 菜单「订单业务」「手术与营销」等能展开 ④ 代金券列表 / 销售订单列表能进入且表格有表头 ④ Console 中无未捕获的 SyntaxError / 5xx 报错 ⑤ 若跳 404 必须先 `localStorage.clear()` + 重新登录排除假 404
- 与 Nginx/前端构建契约：若 `VITE_BASE=/` 则 `/dms/*` 必须 **302 外部重定向**（非内部 rewrite）到 `/*`，避免 URL 前缀与 Router base 不匹配跳 404；若 `VITE_BASE=/dms/` 则文件放 dms 子目录 + alias 直接 serve；两种方案不能混合

**Gap 6：重写页面时凭记忆重建，丢失原有交互与筛选条件（2026-08-27 销退单返工事件）**
- 表现：销退单新增页被重写后，①经销商变成"选择出库单后自动带出"的禁用文本（用户无法先选客户）；②出库单选择弹窗里让用户**手输经销商名称文本**（而不是选择器/锁定主页面客户）；③弹窗原有的**批号、序列号筛选条件消失**。用户验收时三问："我怎么选经销商？为什么还要手写经销商名称？批号/序列号筛选在哪？"
- 根因（向用户给出的理由）：
  1. **重写前没有做功能对照清单**：直接按"理解的业务流程"新建页面，没有先打开旧页面逐项盘点现有交互（筛选条件、列、选择器、按钮），重写=功能减法而用户不知情
  2. **把引用字段做成了自由文本**：经销商是主数据引用，必须用远程搜索选择器（el-select remote），弹窗内禁止再出现客户名称文本框；客户身份只能来自主页面选择，弹窗只读展示
  3. **筛选条件被"顺手简化"**：批号/序列号是追溯查询核心入口，后端 `shipped-outs` 接口本来就支持 batchNo/serialNo 参数，重写时前端漏挂筛选框，等于把已有能力删掉
  4. **正向验收没覆盖"回显页"**：只验了新建提交成功，没验查看页（reasonCode 映射缺失导致退货原因空白、备注被塞入 reason 文本、经销商名缺失）
- 补强（强制）：
  - **重写/改造任何列表页、表单页、弹窗之前**，必须先在真实环境打开旧页面，输出一份「旧功能盘点清单」（所有筛选条件、表格列、按钮、选择器、弹窗字段、必填校验），新页面逐项对照保留；清单缺项=功能丢失，必须显式说明删除理由并经用户认可
  - **引用数据必须用选择器**：经销商/产品/仓库/出库单等外键，一律 el-select（remote）或弹窗选择，禁止可输入的自由文本框；下游弹窗中的上游主体只读展示（el-tag/文本），禁止重复录入
  - **业务前置条件做成按钮门禁**：必须先选经销商才能选出库单 → 未选时按钮 disabled + tooltip 说明，而不是放一个"自动带出"的假字段
  - **验收必须覆盖回显页**：新建/提交成功后，必须再打开查看页/详情页，逐项核对每个字段回显（枚举显示中文 label、外键显示编码+名称、备注不串字段），不能只验提交 200
  - **后端支持的查询参数必须有对应前端入口**：接口支持的筛选参数（batchNo/serialNo 等）前端必须挂筛选框，并实测筛选生效（输入批号→结果只剩对应单据）
  - **浏览器自动化取证**：EP 弹窗按钮在视口外时 `browser_click` 可能静默失败，下结论前先用 `window.__cap`（error/XHR 钩子）+ JS `el.click()` 取证，禁止把自动化点击失败误判为应用 Bug

### 5.4 必跑脚本

```bash
# L1 静态检查 + L2 API + L3 三端深度冒烟 + L4 E2E 回归
npm run test:all

# 只跑某一层：L1/L2/L3/L4
npm run test:all -- --level=4

# 只跑某模块关键词
npm run test:all -- --module=orders

# 单独深度冒烟（PC + 后台 + H5，含按钮点击和错误监听）
node tools/smoke-test.cjs
```

脚本必须自动：
- 三端登录验证
- 遍历所有菜单页面，检查 Console 错误和网络 4xx/5xx
- 点击每个页面的第一个行内按钮（查看/详情），验证弹窗或详情页正常
- 点击顶部「新增」按钮，验证表单弹窗能打开
- L4 业务回归必须执行填写、保存、回读和数据清理
- 输出截图和 JSON 报告

### 5.5 折扣/计价场景测试清单（v4.3.0+ 强制）

所有涉及价格/折扣/代金券的版本，必须逐项测试以下场景：

| 编号 | 场景 | 操作步骤 | 验证点 |
|------|------|---------|--------|
| D1 | 全场折扣 + 代金券 | 创建订单→选产品→应用全场折扣→应用代金券 | 折扣后金额 = 原价 × 折扣率；代金券扣减 ≤ 面值；最终金额 = 折扣后 - 代金券扣减 |
| D2 | 产品折扣 + 代金券 | 创建订单→选产品→应用产品级折扣→应用代金券 | 每个产品折扣独立计算；代金券扣减按比例分摊到每行 |
| D3 | 代金券面值 > 订单金额 | 创建小额订单→应用大面值代金券 | 系统应拒绝或只扣减订单金额，不能超额扣减 |
| D4 | 多产品代金券分摊 | 创建含 3+ 产品的订单→应用代金券 | 每行扣减金额按行金额比例分摊，总和 ≤ 面值 |
| D5 | 代金券 + BOM 母件 | 创建含 BOM 产品的订单→应用代金券 | BOM 子件不独立计价，代金券只作用于母件行 |
| D6 | 审批拒绝后代金券回滚 | 创建订单→应用代金券→提交审批→审批拒绝 | 代金券状态恢复为 ISSUED（可用），可再次使用 |
| D7 | 审批退回后代金券回滚 | 创建订单→应用代金券→提交审批→审批退回 | 代金券状态恢复为 ISSUED，订单回草稿 |
| D8 | 代金券状态校验 | 尝试使用已使用的代金券→尝试使用已过期的代金券 | 系统拒绝并提示业务原因 |
| D9 | 销退流程完整链路 | 选经销商→选出库单→选产品→填可退量→提交 | 必须先选经销商才能选出库单；可退量不能超发货量 |
| D10 | 销退审批后库存回写 | 销退审批通过→检查库存 | 库存增加，锁定释放 |

### 5.6 主数据显示规则（v4.3.0+ 强制）

所有页面（列表/详情/编辑/弹窗）中显示产品、客户、供应商等主数据时，必须遵守：

1. **禁止显示数字 ID**：任何地方不得出现 `dealerId: 5`、`productId: 682` 等原始 ID
2. **必须显示编码+名称**：至少显示编码（如 `PRD-B001`）和名称（如 `测试产品A`）
3. **错误消息必须带业务标识**：如"产品 [PRD-B001 测试产品A] 没有维护有效销售价格"，禁止只抛 ID
4. **前端配置检查**：所有 `modules.js` / `v430-modules.js` 中的列定义，外键列必须使用 `xxxName` 而非 `xxxId`
5. **后端 DTO 检查**：所有返回给前端的 DTO 必须包含 `xxxCode` 和 `xxxName` 字段，不能只返回 `xxxId`

### 5.7 测试方法强制规则（v4.3.0+ 补强）

1. **API 测试 ≠ 功能测试**：API 返回 200 只证明接口可用，不证明功能正确
2. **必须用浏览器验证**：所有 UI 功能必须用 TRAE-browseruse 或 Playwright 真实操作
3. **必须回读数据库**：关键操作后必须查询数据库验证状态变化
4. **必须测试异常路径**：不能只测正向流程，必须测试拒绝/退回/撤销/重复/并发
5. **必须验证数值正确性**：金额、数量、库存等必须回读验证具体数值
6. **必须测试交互顺序**：有依赖关系的选择器必须测试反向顺序

---

## 6. 项目地图（减少重复探索）

### 关键文件

| 用途 | 路径 |
|------|------|
| 列表页统一组件 | `frontend-vue/src/components/CrudView.vue` |
| 独立列表组件 | `frontend-vue/src/components/ListPageLayout.vue` |
| 布局 composable | `frontend-vue/src/composables/usePageLayout.js` |
| 权限指令 | `frontend-vue/src/directives/has.js` |
| 菜单配置 | `frontend-vue/src/config/menu.js` |
| API 封装目录 | `frontend-vue/src/api/` |
| 路由配置 | `frontend-vue/src/router/` |

### 常用命令

```bash
# 前端构建
cd frontend-vue && npm run build

# 后端构建
cd backend && mvn clean package -DskipTests

# 部署到测试环境
python scripts/deploy_test.py

# 深度冒烟测试
node tools/smoke-test.cjs
```

---


---

## 7. 详细规范索引

需要以下信息时直接读对应文件，不要重新探索：

| 信息 | 文件 |
|------|------|
| 核心规则（部署铁律、环境信息） | `.memory/layers/layer1-rules.md` |
| 工程约定（列表页、UI 风格、编号规则等 26 章） | `.memory/layers/layer2-conventions.md` |
| 历史教训（踩坑记录 L01-L41） | `.memory/layers/layer3-lessons.md` |
| 技术决策（D01-D26） | `.memory/layers/layer4-decisions.md` |
| 当前版本上下文 | `.memory/layers/layer5-context.md` |
| 验证清单 | `.memory/checklist.md` |
| 必备 Skill（computer-use / review-loop） | 本文件第 10 节、`.memory/layers/layer1-rules.md` §0.6 |
| 部署 Skill | `.trae/skills/dms-deploy/SKILL.md` |
| 多 Agent 团队模式 | `dms-dev-team/SKILL.md` |

---

## 8. 版本号规则

- 当前版本：**v4.3.2**（v4.3.0 MINOR 功能包 R1–R9 + v4.3.1 走查修复 + v4.3.2 PATCH：销退审批流接入/有价0金额退货联动/销售订单布局与送货地址必填；Flyway 已到 V135；2026-08-27 测试环境已部署验证，生产待发布）。
- PATCH（4.3.0 → 4.3.x）：每次小修复/bugfix 由 Codex 自动升，同步 CHANGELOG 和文档。
- MINOR（4.3 → 4.4）和 MAJOR（4.x → 5.0）：必须由用户明确决定，Codex 不得自动升。
- 每次交付在 CHANGELOG.md 顶部新增条目。

---

---

## 9. 交付汇报格式

每次任务完成后，按以下格式汇报（简洁，不要长篇大论）：

```
## 完成内容
- [逐条列出做了什么，对应需求编号]

## 自检结果
- 代码检查：通过/有问题（说明）
- 浏览器验证：通过/有问题（说明）
- 冒烟测试：通过/失败（失败项）
- Console 错误：无/有（列出）

## 关键假设
- [列出做了哪些默认假设]

## 后续建议
- [可选的下一步]
```

---

## 10. 必备 Skill（强制执行）

> 以下 Skill 已安装到 `~/.codex/skills/`，每次会话自动加载。DMS 相关任务必须按触发时机主动调用，不得跳过。

| Skill | 安装路径 | 何时必须使用 |
|-------|----------|-------------|
| `dms-project` | `~/.codex/skills/dms-project/` | 任何 DMS 仓库内任务的主入口：加载项目形态、流程、UI 不变量、测试标准和记忆索引 |
| `dms-requirement-intake` | `~/.codex/skills/dms-requirement-intake/` | 阶段 A：非平凡需求动手前必须用，输出 R1/R2、影响范围、不做什么、验收标准、关键假设 |
| `dms-ux-functional-audit` | `~/.codex/skills/dms-ux-functional-audit/` | 阶段 C：页面/流程交付前必须用，做 UI、UX、权限、状态、业务价值和反向路径深度审计 |
| `qa-skills` | `~/.codex/skills/qa-skills/` | 深度 QA：UX 评分、adversarial/break-the-app、多用户/移动/桌面 Playwright、韧性/性能/安全审查 |
| `computer-use` | `~/.codex/skills/computer-use/` | 需要真人式真实点击、窗口/浏览器操作和失败恢复的验证场景 |
| `playwright` | `~/.codex/skills/playwright/` | 终端驱动真实浏览器、表单填写、截图、DOM/网络断言，沉淀可重复 E2E |
| `screenshot` | `~/.codex/skills/screenshot/` | 页面、弹窗、报表、异常态和验收证据截图 |
| `review-loop` | `~/.codex/skills/review-loop/` | 代码修改完成后、交付前，对 diff 跑 review→fix→review 闭环 |

### 10.1 阶段 A：需求理解强制规则
- 非平凡需求必须调用 `$dms-requirement-intake`，先完成需求确认再编码。
- 每条需求必须编号为 R1/R2，并写清业务目标、影响范围、不做什么、验收标准、关键假设。
- 遇到价格、库存、可退量、BOM、促销、审批、状态机、多租户、权限、PC/Admin/H5 差异时，必须显式确认或写入关键假设。
- 用户确认“开始/继续/没问题”后自动进入执行，不再反复追问；但执行中发现隐藏需求或假设错误，要先补充 R 项和影响范围再继续。

### 10.2 阶段 B：执行规则
- 用 TodoWrite 逐条跟踪 R1/R2，最终逐条回读。
- 修改前端时遵守 `dms-project` 的 UI 不变量和 `.memory/layers/layer2-conventions.md`。
- 代码改完后先运行本地可行验证，再调用 `$review-loop` 清零可执行问题。

### 10.3 阶段 C：验证与审计强制规则
- 任何影响 UI/交互/业务流程的任务，必须调用 `$dms-ux-functional-audit`，并结合 `$qa-skills` 的 UX/adversarial/mobile/multi-user 规则。
- 真实浏览器验证可根据场景选择 `$computer-use`（探索式/桌面操作）或 `$playwright`（可重复脚本/网络断言）；截图统一使用 `$screenshot` 或对应工具保存到 `automation_test/v4-browser-results/`。
- 验证不能只看 200、构建成功、弹窗打开或 toast；必须回读列表/详情/API/数据库中的业务结果。
- 每个页面/流程至少覆盖：正常路径、空/加载/错误/禁用态、权限/角色、非法输入、反向操作顺序、重复提交；涉及状态/库存/价格/审批时必须回读真实状态。
- 发现缺陷时记录复现步骤、预期/实际、严重级别、Console/网络错误、证据路径和修复建议。

### 10.4 外部方法论评估结论
- `obra/superpowers` 已拉取到 `~/.codex/skill-evaluation/superpowers/` 供参考，最值得吸收的是 `brainstorming`、`writing-plans`、`systematic-debugging`、`verification-before-completion`；未全量启用，避免与本项目 AGENTS.md 冲突。
- `genkovich/sdd` 已拉取到 `~/.codex/skill-evaluation/sdd/` 供参考，最值得吸收的是 `interview`、`clarify`、`specify`、`plan-tests` 的 Socratic 提问和门禁思想；已体现在 `dms-requirement-intake`，未全量启用。

